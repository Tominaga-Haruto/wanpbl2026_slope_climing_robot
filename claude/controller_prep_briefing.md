# コントローラー準備チャットへの指示書（Switch 2 Pro コントローラー → 速度指令）── 2026-09-18 版
 
> 新しい Cowork チャットの最初に読む。恒久ルールは `project_handbook.md`、入口は `README.md`。実機側の最新は `next_chat_briefing_motor.md`。
> 作成の経緯は `chats/2026-09-18_overnight-and-controller.md`。
 
## 0. このチャットのゴール
- Windows ノートPC（`C:\Users\harut\Connect2USB2CAN`）で Switch 2 Pro コントローラーを読み、方策に渡す速度指令 (vx, vy, wz) を 50 Hz で作れるようにする。
- 安全装置（デッドマン、非常停止、途絶で停止、急な変化の制限）を込みにし、モーターを動かさない乾式テストで確かめるところまで。
- 制御ループ ver9（方策・CAN・T265 をつなぐ本体）への組み込みは、ver9 の形が決まってから。今回は「組み込める部品」を作る。
## 1. 最初にやること
1. この文書と `next_chat_briefing_motor.md` の「状況の整理」と「ファイルの場所」だけ読む。T265 の Python の事情は `realsense_t265.md` を `project_search` で引く。
2. ノートPCのフォルダ `C:\Users\harut\Connect2USB2CAN` の接続をユーザーに頼む（Cowork から中を読むため）。
3. ユーザーに聞く: つなぎ方（USB-C 有線 / Bluetooth）、Steam を入れているか、コントローラーを使う Python（ver8 の `py -3.13` か、T265 の `.venv310` か）。
## 2. 前提として分かっていること
- **方策が受け取る指令:** 観測の velocity_commands は (lin_vel_x, lin_vel_y, ang_vel_z) の3つ、胴体（base）座標。学習時は3つとも ±1 の範囲で出していた。実値の範囲・並び・scale は WRS が作る `DEPLOY_README.md`（`D:\Tominaga\deploy_pkg\20260918\`）と `obs_contract.md` で必ず確認する（この文書の数字で決めない）。
- **評価で確かめた指令:** 前進 0.5 m/s（S1）、前進 0.5 ＋ 旋回 ±0.3（S7 / S8 の小旋回）。どちらもデプロイ候補 H_eff13p5@2999 で合格。
- **その場旋回 (0, 0, ±0.5) は H_eff13p5 ではできない。** 夜間の学習で旋回できる方策が出るかもしれないが、実験用（吊って見るだけ）。
- **ユーザーの要望（2026-09-17）:** 旋回ボタン (0, 0, wz) と並進ボタン (vx, vy, 0) を分けたい。「向きを変えて右へ行く」と「前を向いたまま右へ行く（横移動）」を分けたい。下がりながら回る動きもあってよい。
- **モーター側の注意:** 指令が途絶えるとモーターは最後の指令でトルクを出し続ける。だから「コントローラーが止まったら指令 0」だけでは足りず、ループ側が止める動作（ゆっくり止まる → 立つ姿勢 or 脱力）を能動的に送る必要がある。脱力の仕方は実機側（`next_chat_briefing_motor.md`）と合わせる。
- **速度観測（T265）が途切れたとき:** 直前値を保持し、0.2 s で停止動作に入る（学習側の評価結果）。コントローラーの途絶も同じ扱いに揃えるのが自然。
## 3. Switch 2 Pro コントローラーの PC 対応（2026-09-18 に Web で確認した範囲。実機で必ず確かめる）
- USB 有線なら動くという情報が多い。Steam は Switch 2 Pro コントローラーに対応済み（Steam Input で XInput 相当に変換できる）。
- 無線（Bluetooth）は追加のドライバーが要るという報告がある。
- pygame（SDL2 ベース）で直接読めるかは不明。SDL3 系か、Steam Input 経由の XInput で読むのが候補。**どの経路で読めるかを最初に実測して決める。推測で実装しない。**
- 出典: https://windowsforum.com/news/switch-2-pro-controller-usb-works-wireless-needs-drivers.442915/ 、https://www.techradar.com/gaming/the-nintendo-switch-2-pro-controller-is-now-supported-on-steam 、https://www.pcgamer.com/how-to-use-a-nintendo-switch-pro-controller-on-pc/
## 4. 進め方（この順。1段ずつユーザーの実測で確かめる）
1. **認識の確認（コードなし）:** USB-C でつなぎ、Windows のデバイスマネージャーと「USB ゲーム コントローラーのセットアップ」（joy.cpl）に出るか。出ない・ボタンが動かないなら、Steam 経由を試す。結果（デバイス名、VID / PID、軸とボタンが動くか）を記録する。
2. **読み取りの確認スクリプト `controller\pad_probe.py`:** 使う経路（SDL / XInput / HID）で、全部の軸とボタンの生の値を 50 Hz で表示し、ログに残す。スティックの中立のずれ（デッドゾーンの決め方）、最大値、更新周期の揺れ、抜き差ししたときの挙動を実測する。パッケージを入れる場合は、どの Python に何を入れるかを先にユーザーに確認する。
3. **割り当ての決定（ユーザーと相談）:** たたき台:
   - ZR を押している間だけ有効（デッドマン）。離したら指令 0。
   - 並進モード: ZR ＋ 左スティック → (vx, vy, 0)
   - 旋回モード: ZL ＋ 右スティックの左右 → (0, 0, wz)
   - 両方押し: 歩きながら曲がる (vx, vy, wz)。wz は小さめに制限（S7 / S8 で確かめた ±0.3 まで）
   - 非常停止: 決めた1つのボタン（例: − と ＋ の同時押しは誤操作しにくいが、とっさに押しにくい。ユーザーと決める）。一度押したら、ループを再起動するまで解除できない
   - 速度の段階（L / R で 弱・中）。最初は弱だけ
4. **指令を作る部品 `controller\teleop.py`:** 生の値 → デッドゾーン → 上限 → 変化の速さの制限（例: 0.5 m/s² 相当。値は吊り試験で決める）→ (vx, vy, wz) と状態（有効 / 停止 / 非常停止 / 途絶）を 50 Hz で出す。途絶（読み取りが 0.2 s 来ない・コントローラーが外れた）は停止扱い。すべてログ（時刻・生の値・出した指令・状態）に残す。
5. **乾式テスト（モーターなし）:** 画面に指令を出すだけで、各ボタン・途絶（ケーブルを抜く）・非常停止・デッドマンを1つずつ試し、ログで確認する。
6. **ver9 への渡し方の決定:** T265 は Python 3.10、ver8 は 3.13 で分かれている。コントローラーを同じプロセスに入れるか、別プロセスから UDP などで渡すかを、ver9 の構成と合わせて決める。別プロセスにするなら、受け手側にも途絶（0.2 s）で停止を入れる。
## 5. 最初の実機での上限（吊り試験用のたたき台。実機側と合わせて決める）
- vx 0〜0.3 m/s（後退は最初は無効）、vy ±0.2、wz ±0.3。評価で確かめた値（前進 0.5）より下から始める。
- 1モーター → 脚1本 → 全身の順は実機側の手順に従う。コントローラーは全身を吊ったときに初めて方策につなぐ。
## 6. やり残しになりそうなこと（このチャットで決めなくてよい）
- 無線化（Bluetooth のドライバー）
- 振動などのフィードバック（途絶や非常停止を手元に知らせる）
- 立ち止まり (0, 0, 0) の指令で方策が安定して立っていられるかは、シムの S3 と吊り試験で見る
## 7. 対話の注意
- コマンドは実行ディレクトリを明示し、山括弧のプレースホルダは使わず、長いものは1行版も出す。
- 推測で直さない。読み取りの挙動は生ログで実測してから決める。
- 実機のモーターを動かす段では、何が起きるかを先に説明し、吊る・トルクを絞る・非常停止を用意する。
- 終わる前に `chats/YYYY-MM-DD_件名.md` を作り、README の索引に1行追加する。
 


## 追記
### プロコンに書かれてたこと：
- Wireless Controller
- Input:DC 5V 500mA
- Battery Capacity:1000mAh
- Made in China
### ボタンの対応：
- 左のスティック（or十字キー）で速度指令を送る。（旋回できそうなら右のAボタンで時計回りに旋回、Yボタンで半時計周りに旋回で行こうかな。）（もちろん速度指令だけで旋回できるpolicyならそれでもいい）
### わかってること:
- 友達が少ししらべてくれた。パソコンにつないでパソコンを操作をすることはできる。
- HID準拠ゲームコントローラーらしい。