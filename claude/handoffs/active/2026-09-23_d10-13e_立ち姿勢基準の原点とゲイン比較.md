# 引き継ぎ: D10-13E（原点の中止を立ち姿勢から、転倒は傾きで止める、ゲイン ×1.0 / ×0.7 の交互比較）と WRS 実験14

作成 2026-09-23 / 対象: 次のチャット / 前任: D10-13D の判定と D10-13E の実装（`../../reports/2026-09-23_d10-13d_result_原点補正と方策の最初の1秒.md`）

## 最初の仕事

1. **ユーザーが Onshape で測った膝の角度**（CAD の組立姿勢、左右）と、足裏が胴と平行かを受け取る。
   - URDF の順運動学では左 12.3°・右 7.2°。R1 は補正で立てたので、大きく違わなければ `cad_fk` のまま。
   - 大きく違うなら `ZERO_OFFSET_PRESETS_DEG["cad_fk"]` を測った値で作り直す（新しいプリセット名を足す。既存は変えない）。
2. **`../../procedures/d10_13e_立ち姿勢基準の原点_傾き中止_ゲイン比較_実験手順.md` の結果**（E1〜E4 = vx 0.2 を ×1.0 / ×0.7 交互、E5 = vx 0.4）を、自分でユーザーの PC で解析する。
   - Cowork では `C:\Users\harut\Connect2USB2CAN` をフォルダアクセスで繋ぎ、VM で `pip install --user python-can onnxruntime`、`python3 ver9_d8_sender.py --analyze logs/<CSV>`。
   - 判定は `POLICY START:` の行（peak |pitch| in 0.5s）を A 2 本・B 2 本で比べる。手順書の表に従う。
   - 方策の中身は `_obs.csv` を tick ごとに見る（pitch・ang_vel y・pos_rel と行動の HFE/KFE/FFE）。
3. **D10-13D で左脚の CAN が落ちた件**（`MIT send failed 0x2B` → 左脚 5 軸が 3 回見つからない → 4 分後に復帰）について、ユーザーが何をして戻したかと電源の設定を聞く。再発したら電源の電流を測るまで先へ進まない。
4. **WRS 実験14** の報告が来たら、S0 の最初の 0.5 s の pitch と、実機（0.3 s で +14〜+25°）を比べる。S1（剛性 ×1.5）だけが実機に近ければ、D10-13E の ×0.7 と合わせて Kt の仮説を判断する。

ユーザーは時間がなく、「本質的な実験を先に、無理なら戻る」方針。判定に従って、次の実験の実装・手順化まで一気に進める。

## 状況

- 送信器 build `D10_13E_TILTGAIN_20260923_2200`（`.bak_20260923_tiltgain`、テスト 186本 OK）。
  - `--origin-ref stand`: 原点 45°を立ち姿勢（既定姿勢＋補正）から測る。D7 から 75°の上限と膝の逆折れ 10°は残る。`--walk-limits` と `--tilt-abort-deg` が必須。
  - `--tilt-abort-deg T`（15〜40、手順では 30）: 方策の段階で T265 の |pitch| か |roll| が T を超えたら中止。中止後保持の対象。
  - `--policy-gain-scale S`（0.5〜1.0）: 方策の段階だけ Kp・Kd ×S。保持・ゲート・中止後保持は ×1.0。
  - `--analyze` に `POLICY START:` の 1 行。
- **D10-13D のまとめ:**
  - R1: 補正した保持だけで 20 s 立った（pitch −3.8°で静止）。足首は目標よりつま先上げ側へ 10〜17°たわみ、1 本 3.2〜5.6 A で支えている（重心が足首より前。URDF の推定の 2〜3 倍）。
  - 方策は毎回同じ動き: 最初の tick から FFE +0.6〜1.2・KFE −0.4〜−0.9・HFE +0.04〜0.22、0.3 s で前へ 14〜25°。行動は FFE +3〜+10 まで膨らむ。
  - R3 は自分で +1°まで立て直して右脚を踏み出したが、LR_KFE が既定姿勢 +18°（D7 +45.4°）で「原点 45°」に止められた。これが D10-13E の直接の理由。
  - R4 は前 +2.8°で始まり、0.46 s で 30°を超えた（本当の転倒）。vx は実質入っていない。
- WRS 実験14 は未実行（`../../instructions/active/wrs_experiment14_start_replay_instruction.md`）。
- 物理の Kt は未測定。電源の電流はまだ一度も報告がない。

## 注意（Cowork）

- VM の git は commit できない。`git --no-optional-locks` を使い、commit と push のコマンドをユーザーに出す。commit は `-- <パス>` の形で自分の変更だけ。
- **VM から `--no-optional-locks` なしで `git status` を打つと `.git/index.lock` が残る**（2026-09-23 に `Connect2USB2CAN` で起きた。消せない）。ユーザーのコマンドの頭で `Remove-Item .git\index.lock -ErrorAction SilentlyContinue` を付ける。
