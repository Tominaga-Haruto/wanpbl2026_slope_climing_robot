# 引き継ぎ: D10-13D（原点を CAD の姿勢へ読み替える。方策は Ctrl-C で止めない）と WRS 実験14

作成 2026-09-23 / 対象: 次のチャット / 前任: D10-13C の判定、トルク設定・基準姿勢の点検、D10-13D の実装（`../../reports/2026-09-23_d10-13c_result_トルクと基準姿勢.md`）

## 最初の仕事

1. **ユーザーが Onshape で測った膝の角度**（CAD の組立姿勢で、太ももとすねのなす角。左右）と、足裏が胴と平行かを受け取る。
   - URDF の順運動学では左 12.3°・右 7.2°。
   - 大きく違うなら `ZERO_OFFSET_PRESETS_DEG["cad_fk"]` の前提が崩れる。
2. **`../../procedures/d10_13d_原点補正と方策を止めない_実験手順.md` の結果**（R1 補正した保持だけ、R2 vx 0、R3 vx 0.2、R4 vx 0.4）を、自分でユーザーの PC で解析する。
   - Cowork では `C:\Users\harut\Connect2USB2CAN` をフォルダアクセスで繋ぎ、VM で `pip install --user python-can onnxruntime` のうえ `python3 ver9_d8_sender.py --analyze logs/<CSV>`。
   - ファイル: `.csv`、`_obs.csv`、`_abort.txt`、`_timing.csv`（保持・ゲート・方策の pitch とループ周期）、`_meta.txt`（起動行・原点補正）。
   - 方策が何秒続いたかは `stage=policy` の tick の差で見る。**本人の「立ってた」は保持・ゲートの段階のことが多い**（3回続いた）。
3. **WRS 実験14**（`../../instructions/active/wrs_experiment14_start_replay_instruction.md`）の報告が来たら、実機の最初の 1 s（0.2 s で前へ 9〜13°、FFE 行動 +1.0〜1.2）と比べる。
   - シム S0 も同じなら、方策の癖。
   - S1（剛性 ×1.5）だけが実機に近いなら、実機のトルクが強すぎる。次は送信器で Kp・Kd を下げるスケールを入れる。
   - S3（原点の仮説）が近いなら、`--zero-offset` を標準にする。

ユーザーは時間がなく、「本質的な実験を先に、無理なら戻る」方針。判定に従って、次の実験の実装・手順化まで一気に進める。

## 状況

- 送信器 build `D10_13D_ZEROOFFSET_20260923_2300`（`.bak_20260923_zerooffset`、テスト 178本 OK）。
  - `--zero-offset cad_fk`: sim の角度 = D7 原点からの角度 ＋ 補正（LL/LR: HFE +2.9 / −0.9、KFE −12.3 / −7.2、FFE +9.4 / +8.1）。原点 45°の中止は補正前の角度で見る。
  - 保持姿勢へ ramp する run は、起動時の脚のずれを 45°まで許す。
  - 中止メッセージから「`oa` をやり直せ」を外した。
  - `_meta.txt` を追加。
  - 前 build から: `--start-gate-deg`、`--fine-timer`、`_timing.csv`、CSV 名の自動変更、`--walk-speed-abort-dps`（≤400）、`--soft-stop-seconds`。
- **D10-13C のまとめ:**
  - ゲートと 1 ms タイマーは効いた（周期 20.0 ms、T265 の遅れ 0〜3 ms）。
  - 方策は直立から始めても 0.2 s で前へ 9〜13°倒れる。8 run すべて Ctrl-C で 0.2〜1.3 s。
  - R2・R3 は歩く指令が入る前に止めていた。
  - 方策は、理想の既定姿勢・直立・静止でも最初に FFE +0.67 を出す（ONNX で確認）。
- **トルクの点検:** ファームのトルク単位ではゲインはシムと一致。物理の Kt は未測定（AK80-9 0.59 / 0.78 / 0.855 N·m/A）で、物理の剛性はシムの 1.0〜1.6 倍の可能性。分銅での Kt 測定はまだ残っている。
- 電源の電流はまだ一度も報告がない。

## 注意（Cowork）

- VM の git は commit できない。`git --no-optional-locks` を使い、commit と push のコマンドをユーザーに出す。commit は `-- <パス>` の形で自分の変更だけ。
