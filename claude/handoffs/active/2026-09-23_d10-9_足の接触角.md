# 引き継ぎ: D10-9（HAA を閉じて、実機の足が当たる角度を測る）

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-8 の判定と D10-9 の実装（`../../reports/2026-09-23_d10-8_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_9_HAA閉じ_足の接触角_実験手順.md` の R0〜R2 の結果を貼る。** 画面ログ、`--analyze` の出力（`HAA SWEEP (D10-9)` の節）、CSV 名、`oa` 直前の足裏の隙間（cm）、当たった部位の写真、動画、電源の電流がそろっているはず。ユーザーの PC で `--analyze` を掛け直し、手順書の判定表で判定する。

## 状況

- 送信器 build `D10_9_HAASWEEP_20260923_1500`（`.bak_20260923_haasweep`、テスト 138本 OK）。`--haa-close-deg D`（`--stand-only` 専用、0 < D ≤ 16）: 初期姿勢で保持 → 両 HAA を 2°/s で −D°へ → 目標より 4° 遅れが 10 tick 続いたら両 HAA をいまの位置に固定 → 2 s 保持。`--analyze` は `HAA SWEEP` の節を出す。
- **D10-8 の判定:** R1（vx 0）は判定1（KFE の SLEW GAP 27〜30°、D10-7 の半分以下）。R2（vx 0.2）は判定2（145〜154°、D10-7 と同じ。吊りではこれ以上分からない）。判定3（傾き）には当たらない。中止なし。
- **主な発見:** シムは URDF 上の足首間隔 約 18 cm（HAA −14〜−16°）で当てずに歩く。実機は 24〜29 cm（HAA −4〜−9°、R1 は LR_HR −10° のつま先内向きあり）で当たった。D10-9 はこれが形（または原点）の差か、HR・振り回しのせいかを分ける。
- URDF の順運動学は `robot_sim.urdf` の `<axis xyz="0 0 -1">` を必ず入れる（読み落とすと HAA の向きが逆に見える。D10-8 報告に記録）。
- 電源の電流はまだ一度も報告がない。

## 判定後の分岐

- 判定1（−14°まで当たらない）→ 床上の手順書を書く。slew の扱いと電流中止値（D10-5〜D10-8 の実測）を決める。
- 判定2・3（当たる）→ 床に進まない。WRS 機の CLI に、USD の足メッシュどうしの距離を HAA ごとに計算させる（自己完結プロンプト、`instructions/active/`）。実機の測定と突き合わせる。
- 判定4（再現しない）→ 原点の支持治具が先。

## 許可／禁止

手順書の「許可／禁止」に従う。R1・R2 は要承認。方策の run と床上の run は禁止。

## 注意（Cowork）

VM の git は `.git` 内のファイルを消せないので、commit できない。`git --no-optional-locks status` を使い、commit と push のコマンドをユーザーに出す。VM で python-can を使うときは `pip install --user python-can`。
