# 引き継ぎ: X20（左右交互の歩き方を教える学習 2 本）

作成 2026-09-27 03:00 / 前任: 2026-09-25〜27 のチャット（記録 `../../chats/2026-09-25_シムの歩き方の修正と学習し直しX18.md`）。
モーターは下から 1 = 足首（FFE）、2 = 膝（KFE）、3 = 股の前後（HFE）、4 = 股の開閉（HAA）、5 = 股のひねり（HR）。本人にはこの呼び方で話す。

## 状況（ここまで）

- 実機で床の歩行ができた方策はまだ無い。H_eff13p5@2999（第一候補）は、シムで「膝を曲げたまま 1 秒に約 10 歩のすり足」で、実機の電流・速度の中止に掛かった。
- 2026-09-25 の学習し直し X18（全部ゼロから、または X18 どうしで再開。**H2999 からの再開は一度もしていない**）は、立往生 4 本、報酬を大胆にした X18h は両足跳び。考察は `../../reports/2026-09-25_学習し直しX18の結果と考察.md`。
- 結論: どの報酬も「左右交互に足を出す」を教えていなかった。**X20 はそこを直接直す。**
- 本人は発表資料も並行している（発表スライドの引き継ぎは別: `2026-09-25_PBL発表スライド_ver5_2以降の引き継ぎ.md`）。

## いまやっていること: WRS で X20 を 2 本（ゼロから、各最大 3000 iter）

指示書 `../../instructions/active/wrs_experiment20_gait_clock_instruction.md`（新しい CLI チャットに貼るブロックとコードつき）。コードは `my_robot_code/stand_env_cfg.py` の末尾「X20」節。

共通（`_X20BaseEnvCfg`）: X18 のまっすぐ寄りの立ち姿勢（膝 8°、CAD の左右非対称を補正、δ=+1.5）、床に着いた高さ 0.377 m から速度 0 で開始、膝・5番目の可動域は X18 のまま、**物理は H に戻す**（トルク上限 3・1番目 13.5 N·m・他 53、4番目の下限 −16°）、正則化は H 並み、足ごとの一歩の報酬（両足跳びで 2 倍もらえた穴）は 0 にする、速度誤差の直線の罰 −1、両足同時に浮くことの罰、転倒 −10、指令 vx −0.2〜0.6・vy ±0.1・wz ±0.3。

| | **X20a 詰め込み版**（`GaitFull-v0`） | **X20b 適度版**（`GaitMod-v0`） |
|---|---|---|
| 観測 | **44 次元**（最後に 0.7 s 周期の時計の sin・cos） | 42 次元（H と同じ） |
| 左右交互 | 時計の前半は左足・後半は右足が接地、に報酬 2.0（止まれの指令では両足接地が正解） | もう片方の足が接地しているときだけ数える一歩の報酬 2.0（0.15〜0.4 s）＋片足支持 1.0 |
| 振り足 | 時計が「空中」の足が、立ったときより 4 cm 上がっていないと罰 −20 × 不足 [m] | なし |
| 両足同時に浮く | −2.0 | −1.0 |
| 両足着きっぱなし | なし（時計の報酬が兼ねる） | −1.0（0.4 s 超） |
| 止まれの指令 | 10%、既定姿勢からのずれを罰 −0.5 | 5% |
| 速度追従 | 2.0 / 幅 0.25 | 2.0 / 幅 0.35 |
| 実機に載せるとき | 送信器に「時計の sin・cos を観測の最後に足す」処理が要る（未実装） | 観測はそのまま |

両方とも、実機に載せるには**送信器の既定姿勢を新しい立ち姿勢に**変える必要がある（未実装。今の送信器は H の −10 / +20 / −10 前提）。

## 次のチャットの最初の仕事

1. 本人が CLI の報告（S1 の FOOT_Z_STAND と足の順、S2 の合否、学習コマンド）を貼ってくる。学習コマンドを確認して渡す。GPU は 1 枚。1 本目を起動し、1〜2 iter 後に nvidia-smi で空きを見て 2 本目。
2. **500 iter 前後で再生して判定**（本人の目視）:
   - 左右交互に足を出して前へ進む → 当たり。1000〜1500 で評価コマンド（歩行周期・膝の平均・角速度とトルクの p99 を H と並べる）。
   - 立往生・両足跳び・すり足のどれか → その名前で記録。X20a が交互、X20b がだめなら「時計が要る」が結論。
3. ターミナルで見る値: X20a は `gait_contact`（1 に近づけば時計どおり）と `no_flight`（0 に近づく）、X20b は `feet_single_air_time`（正で増える）と `double_support`（0 に近づく）。

## 当たりが出たら（実機へ）

- 送信器（`C:\Users\harut\Connect2USB2CAN`、ブランチ **`feat/mit-mode`**、最新 build `D10_13L_WIDE_20260925`）に、新しい立ち姿勢（X20 の既定姿勢）と、X20a なら時計の観測を足す。パッケージ化は `tools/dump_contract.py`・`verify_onnx.py` の流れ（既存）。
- 自動停止は `--wide-limits`（D10-13L、未実行）を使える: 電流中止 1番目 24・2番目 16・3番目 24・4番目 10・5番目 6 A、速度中止 900°/s、slew 上限 1000°/s。手順 `../../procedures/d10_13l_安全制限をシムの歩行並みに_実験手順.md`。
- 実機の 4番目は −8.5〜−11°で足先が触れる。X20 は −16°まで許しているので、実機では `--safe-clamp`（4番目を −8°で止める）を併用。

## だめだったら（次の一手）

1. 左右対称のデータ拡張（rsl_rl の symmetry。関節の符号の対応表が要る: `robot_joint_map.py` の JointBinding.sign）。
2. カリキュラム: まず H の立ち姿勢・開始で X20a の報酬を入れて交互に歩けるか確かめ、そのあと立ち姿勢を変える。
3. 時計が作る基本の歩行軌道＋方策は補正だけ、の方式。

## 注意

- 文書リポジトリで `git add -A` や `git add claude/instructions` のようなフォルダ指定をしない（他のチャット由来の未追跡ファイルを巻き込む。2026-09-25 に一度やって amend で外した）。
- 2026-09-27 時点で `claude/CONTEXT.md` に別チャット（Jetson 版送信器の点検）の未コミットの 1 行がある。そのチャットの持ち物なので、こちらの commit には入れない（index だけ自分の変更にして commit した）。
- Connect2USB2CAN は `feat/mit-mode` に push する（`main` に push すると non-fast-forward で弾かれる）。
- VM から commit するときは、ロックファイルを消す許可（device_request_delete_permission）が要る。
