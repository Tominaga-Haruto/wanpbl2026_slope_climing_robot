# 引き継ぎ: D10-13（床の上で方策に任せて立たせ、歩かせる。前傾合わせは飛ばした）

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-10 の判定、D10-11・D10-12・D10-13 の実装（`../../reports/2026-09-23_d10-10_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_13_床上方策_立ちと歩行_実験手順.md` の結果を貼る**（R0、R1 vx 0 を 5 s、R2 vx 0.2 を 10 s、R3 vx 0.4 を 15 s。どこまで進んだかは判定次第）。

- ユーザーは `--analyze` を打ち忘れることが多く、画面ログも抜けがち。**自分でユーザーの PC で全 CSV に `--analyze` を掛けて**、手順書の判定表で判定する。
- 中止理由は画面ログにしか出ない。無ければ CSV の最後の行（中止値に近い電流・速度の軸）から推定し、推定と明記する。
- **ユーザーは時間がなく、「本質的な実験を先にやり、無理なら戻る」方針。** 判定に従って、次の実験を実装・手順化まで一気に進める。

## 分岐

- **立てた・歩けた（判定1・4・6）:** 手順書の「このあと」の順に進む。D10-14（duration 上限を 30〜60 s に、vx 0.5 と停止の切り替え、手の支えを外す）→ D10-15（wz・vy）→ D11（`../../procedures/d11_jetson_無線操作_平地デプロイ実験手順.md`、無線コントローラー・Jetson）→ 紐なし。
- **R1 で 1〜2 s で後ろへ倒れる（判定2）:** 戻り先1は D10-12（`../../procedures/d10_12_自動前傾_床上方策_実験手順.md` の R2・R3。`--auto-lean-deg 8 --walk-limits`）。戻り先2は D10-11（`../../procedures/d10_11_床上前傾スイープ_実験手順.md`）。どちらも実装済み・未実行。
- **足踏みするが進まない（判定5）:** friction DR 版の重み（実験12・13、`../../instructions/`）を試す候補。
- **中止（判定3）:** これ以上の上限は上げない。別に承認を取る。

## 状況

- 送信器 build `D10_13_FLOORWALK_20260923_1800`（`.bak_20260923_floorwalk`、テスト 158本 OK）。
  - `--walk-limits`: KFE・FFE 10 A、速度中止 200°/s、slew ≤200°/s、duration ≤15 s。
  - `--command-delay-seconds D`（≤5 s、方策開始から D 秒は指令 0）。
  - D10-12 の `--auto-lean-deg`、D10-11 の `--stand-knee-deg` / `--stand-lean-deg` / `--lean-sweep-deg` も入っている。
- **D10-10 のまとめ:**
  - 床に降ろすと後ろへ倒れる。足首はつま先が上がる向きに 5〜9°押される。
  - 方策は倒れを止める向きに動いたが、FFE 6 A（推定）と slew 30°/s に止められた。
  - URDF の重心は足首より 1.5 cm 前。実機の重心はモデルより後ろ（仮説）。
- AK80-9（HFE・FFE）の CSV の電流の符号は信用しない。判定は位置のずれで行う。
- ねじの緩み: 0x11・0x1B・0x2B（本人報告）。
- 電源の電流はまだ一度も報告がない。床上の歩行では必須。

## 許可／禁止

手順書の「許可／禁止」に従う。R1〜R3 は要承認。吊り紐なし、vx > 0.4、vy・wz ≠ 0、`--walk-limits` を超える値は禁止。

## 注意（Cowork）

- VM の git は `.git` 内のファイルを消せないので、commit できない。`git --no-optional-locks status` を使い、commit と push のコマンドをユーザーに出す。
- 他の PC の変更がステージされていることがある。commit は `git commit -m ... -- <パス>` の形で、自分の変更だけにする。
- VM で `--analyze` するときは `pip install --user python-can`。
