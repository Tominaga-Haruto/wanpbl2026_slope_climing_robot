# 引き継ぎ: PBL 発表スライド（制御パート）ver5 以降

作成 2026-09-25 05:50 JST（発表は本日）。前任: ver4 作成チャット（`chats/2026-09-25_pbl-control-slides-ver4.md`）。
**ver4 は本人のノートの修正指示まで反映済み。** 次のチャットは本人の新しい修正（ver5〜）を受けて直す。旧引き継ぎ `handoffs/inactive/2026-09-24_PBL発表スライド_制御パート引き継ぎ.md` の方針（下の 2）は有効。

## 1. いまのファイル

| もの | 場所 | 状態 |
|---|---|---|
| **ver5（最新）: Slides アーティファクト「制御パート ver5」** | https://claude.ai/artifact/RBDVnQFyYYSfdjNoENT2ZK （pptx・PDF でダウンロード可） | 本人の `PBLプレゼンテーション_ver5のための` pptx（制御 14〜23 の 10 枚）を、構成・色を保って作り直したもの。id: overview loop rl cad reward struggle simresult realresult discussion future。直すのはこちら |
| ver4_2 | https://claude.ai/artifact/5Vemea8DrFSMaVZRfuY43q | ver5 で置き換え済み |
| ver4（1 つ前） | https://claude.ai/artifact/Diedeh6qTS1qJPnGsL5fHm | ver4_2 で置き換え済み |
| ver4 の生成スクリプト | `tools/presentation/build_control_slides_ver4_artifact.py` | スライド HTML と deck.json を出す。**本人がアーティファクト上で手直ししていたら、再生成で上書きしないこと**（下の 3） |
| ver4 の入力（本人がノートに修正指示を書いたチーム全体の pptx） | `claude/output/制御パート_ver4入力_ユーザー編集ノート付き.pptx` | 制御パートは 24〜41 枚目 |
| チームデッキ PDF | `C:\Users\harut\Downloads\二足歩行ロボット_災害現場向け開発発表_ver4のための２.pdf` | スライド面のみ（**PDF にはノートが入らない**。ノートを見るには pptx 本体をもらう） |

スライド id（順）: overview(14) loop(15) rl(16) cad(17) reward(18) struggle(19) simresult(20) deployA(21) deployB(21 別案) realresult(22) discussion(23) future(24) summary(25, チーム共通まとめの差し替え案)。ノートは各 section 最後の `<aside>`。

## 2. 方針（変えない）

- 本人の文言を優先。明らかな間違いだけ直す。講義にしない。聴衆は教授と B2。全体 10 分強。文字は少なめ、言いたい項目から上に。
- 用語: **ポリシー**（「方策」「ニューラルネット」と言わない。「ロボットの次の行動を決めるルールのようなもの」）、**ご褒美・罰**、Jetson は「ミニコンピューター」、「Switch 2 のプロコン」。
- 発表に使うシム動画・報酬は H_eff13p5@2999（歩きながら旋回だけ L_angstd_w1@4000）。B_direct@25000 は足首を回しきった姿勢なので使わない（ver5 で変更）。
- 実機は誇張しない。**床で 20 s 立てたのは PD 保持（決まった姿勢を保つ制御）でポリシーではない**。ポリシーは荷重を減らして最長 5.3 s。
- 実機で歩けない原因（2026-09-25 更新）: 1 送信器の安全制限（電流・速度）がシムの普通の歩行より狭い、2 シムの歩き方が膝を曲げたまま 5 Hz のすり足、3 空中から落とす開始、4 摩擦 2〜3 倍。**足同士の衝突は主因ではない**。根拠 `reports/2026-09-25_実機で歩けない原因の再検討_安全制限とシムの歩行.md`。学習し直し X18 は立ち止まり・両足跳びで打ち切り（`reports/2026-09-25_学習し直しX18の結果と考察.md`）。
- 回答は日本語。関節は本人との会話では「下から○番目のモーター」。

## 3. 直し方

- アーティファクトを Artifact ツールで read（`project/deck.json` と直すスライド）→ 保存されたファイルを編集 → 同じ url に publish（変えたファイルだけ）。本人がブラウザで直していることがあるので、**必ず read した最新版を直す**（スクリプト再生成は本人の手直しを消す）。
- 本人が PowerPoint で直してきたら、pptx 本体をもらって python-pptx でノートとスライドの文字を読む。

## 4. ver4 でやったこと（本人の指示すべて反映済み）

- 番号の連番化、右上の章番号と左下「PBL 二足歩行ロボットの開発」を削除（左下は他班にもあるのでチームでそろえる、と伝え済み）。
- ① 全体像: ポリシー＝次の行動を決めるルール、Jetson にインポート、Switch 2 のプロコンのスティック。② ループ: 本人の端的版を採用、写真枠 4 つ（プロコン・T265・Jetson・モーター）。
- ③ 強化学習: 「どの行動にご褒美／罰」、「次の段階は前の段階で覚えたポリシーから練習を続けた」。
- ④ CAD: Isaac Sim にインポート、つまずき 1＝Onshape の書き出し機能だとモーター内部まで約 1,300 リンク → onshape-to-robot でまとめた、2＝前後左右の入れ替わり。左右の回る向きは省略。
- ⑤ 報酬: 「重みで、そのまま重要度の順ではない」に簡略化。
- ⑥ カニ歩きスライドの下の帯を削除。
- ⑧ デプロイ: 「今は床で歩かせるところで詰まっている」、安全の自動停止はノートに短く。別案 B（学習用PC→ノートPC→Jetson の流れ＋4 段階の写真・動画枠）。
- ⑨ 「直したものは結果、直していないものは考察」（本人案）: 旧「シムとの違い」スライドを廃止。CAD のまっすぐのずれ（読み替えで床で立てた）は結果へ、残りの 4 原因は考察の表（重要な順・対策つき）へ。関節の向き逆は削除。
- ⑩ 結果: 「荷重を減らしてポリシーに前進の指令を送ると歩こうとする」、立てたのは保持の制御、プロコン無線は確認済み。
- 11 展望: 平地でまっすぐ → Jetson に載せて Switch 2 のプロコン → 坂・でこぼこ。

## 4b. ver4_2 でやったこと（2026-09-25 06 時、同じ入力 pptx を見直し）

- 間違いの修正: T265 が測るのは「胴体の速さ・傾き」（観測は速度・角速度・重力の向き。加速度・位置は使っていない）。「何百万回」→「何度も（ポリシーの更新は合計 25,000 回）」。実際の試行は 4096 体 × 24 step × 25,000 回 ≈ 25 億 step。デプロイ A の緑のカードの色指定の誤り（`#` 抜け）。
- つじつま: 床で立てたのは「決まった姿勢を保つ制御」（ポリシーではない）を結果スライドに明記。報酬の最終形（B_direct）の足上げは従来の feet_air_time +2.0 で、立ち往生の対策（片足で立つ時間＝biped）は H の段階。報酬スライドのノートに質問用の一文。
- 見た目: ループを 4 枚の写真枠が横に並ぶ流れ＋下にフィードバックの帰り矢印に作り直し。デプロイ別案 B を階段の形（1 台ずつ→吊って全身→床で立つ→床で歩く＝いまここ→Jetson＋プロコン）に作り直し。文字の少ない言い方へ。オレンジ・水色の文字は濃くしてコントラスト確保。
- 番号: 14〜25（デプロイ A/B はどちらか一方を使うので両方 21）。後ろの参考文献は 26、最後は 27 にチームで直す。

## 5. 残り・候補

- 動画・画像の貼り込み（本人）。デプロイ A/B どちらを使うか本人が選ぶ。
- 想定問答集（Claude Docs）は ver1 のまま。ver4 に合わせるなら安全制限・シムの歩き方・保持で立った、ポリシーの言い方を入れる。
- git: 前のチャットの VM からは commit できなかった（user.name 未設定）。本人にコマンドを渡している。

## 4c. ver5 でやったこと（2026-09-25 10 時、本人の ver5 用 pptx のノートの指示）

- ノートの指示: CAD のつまずき 2 つ目（前後左右の入れ替わり）を削除、報酬の「目標との差×重み」の言い方を整理、シム結果から坂 15°・その場旋回・でこぼこを削除。
- 情報の更新: シム結果は実験17 の採用動画に合わせて 平地・坂 5°・歩きながら旋回（坂 10°は H では転倒が多い）。報酬スライドを H の値に（回る +0.5、片足で立つ時間 +0.25。B の「足を浮かせて着地 +2.0」は消した）＝立ち往生の対策と一致。カニ歩きの原因は「横が『前』になっていた」、対策は「横へずれると点が大きく減る採点」（追従の幅を狭めた）。
- 考察: 4 原因の表の右列を「学習し直すときに入れた対策」にし、X18 の結果（立ち止まり → 両足跳び）と「左右交互を教えていなかった」を追加。すり足は 1 秒に約 10 歩（5.1 Hz）に訂正。展望 1 は「左右交互に足を出すリズムを与えて学習し直す」。
- その他: T265 のノートを「速さと傾き」に訂正、報酬の負の数が 2 行に折れる・実機の動画枠が下にはみ出す表示崩れを直した、左下の「PBL 二足歩行ロボットの開発」を全スライドに。
