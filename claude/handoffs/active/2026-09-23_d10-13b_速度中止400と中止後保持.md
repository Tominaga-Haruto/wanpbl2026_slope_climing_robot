# 引き継ぎ: D10-13B（速度中止 400°/s、中止したら 3 s その場で保持）

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-13 の判定と D10-13B の実装（`../../reports/2026-09-23_d10-13_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_13b_速度中止400_中止後保持_実験手順.md` の結果を持ってくる**（R1 vx 0 を 10 s、R2 vx 0.2 を 10 s、R3 vx 0.4 を 15 s）。

- **自分でユーザーの PC で全 CSV に `--analyze` を掛けて判定する。** ユーザーの解析出力は信用しない（本人の申告）。
  - Cowork では `C:\Users\harut\Connect2USB2CAN` をフォルダアクセスで繋ぎ、VM で `pip install --user python-can` のうえ `python3 ver9_d8_sender.py --analyze logs/<CSV>` を実行する。
- 中止の理由は `logs/<CSV名>_abort.txt` にある（この build から）。`--analyze` の冒頭にも `ABORT RECORD` として出る。
- 方策の段階が何秒続いたかは、CSV の `stage=policy` の最初と最後の tick の差で見る。**本人の「立てた」は、保持（stand-hold）の段階の話であることがある**（D10-13 で食い違った）。
- ユーザーは時間がなく、「本質的な実験を先に、無理なら戻る」方針。判定に従って、次の実験の実装・手順化まで一気に進める。

## 分岐

- 立てた・歩けた → D10-14（duration 30〜60 s、vx 0.5 と停止の切り替え、手の支えを外す）→ D10-15（wz・vy）→ D11（`../../procedures/d11_jetson_無線操作_平地デプロイ実験手順.md`）。
- 400°/s でも速度中止、または後ろへ倒れる → D10-12（自動前傾）の R2 に `--walk-speed-abort-dps 400 --soft-stop-seconds 3` を足す。
- 足踏みするが進まない → friction DR 版の重み（実験12・13）。

## 状況

- 送信器 build `D10_13B_SOFTSTOP_20260923_1900`（`.bak_20260923_softstop`、テスト 165本 OK）。
  - `--walk-speed-abort-dps S`（`--walk-limits` 専用、200〜400、既定 200）
  - `--soft-stop-seconds T`（`--walk-limits` 専用、0〜5）: 方策の段階で速度・電流・原点の中止が起きたら、止まった位置を STAND HOLD のゲインで T 秒保持してから zero MIT。stale・モーターエラー・電流超過ではすぐ zero MIT。CSV の stage は `soft-stop`。
  - 全 run で中止の理由を `<CSV名>_abort.txt` に書く。
- **D10-13 のまとめ:** 方策の段階は 0.2〜4.3 s ですべて中止した（推定: 速度 200°/s）。方策が始まる時点で、体は後ろへ約 5°、足首はつま先が上がる向きに 8〜9°押されている。方策はすぐ FFE を +29°踏み込み、0.3 s で関節が 190°/s に達する。「膝から崩れた」のは中止後の zero MIT による。
- AK80-9（HFE・FFE）の CSV の電流の符号は信用しない。
- 電源の電流はまだ一度も報告がない。

## 許可／禁止

手順書の「許可／禁止」に従う。R1〜R3 は要承認。速度中止 400°/s を超える値は禁止。

## 注意（Cowork）

- VM の git は `.git` 内のファイルを消せないので commit できない。`git --no-optional-locks status` を使い、commit と push のコマンドをユーザーに出す。
- commit は `git commit -m ... -- <パス>` の形で、自分の変更だけにする。
