# 引き継ぎ: D10-11（床の上で前傾を足して立てる角度を測り、その姿勢から vx 0 の方策）

> 置き換え済み（2026-09-23）。D10-11・D10-12 は未実行のまま、戻り先として残した。後継は `2026-09-23_d10-13_床上方策_立ちと歩行.md`（これも置き換え済み）。

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-10 の判定と D10-11 の実装（`../../reports/2026-09-23_d10-10_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_11_床上前傾スイープ_実験手順.md` の R0〜R3 の結果を貼る。** ユーザーは `--analyze` を打ち忘れることが多く、画面ログも抜けがち。**自分でユーザーの PC で全 CSV に `--analyze` を掛けて**、手順書の判定表で判定する。中止理由は画面ログにしか出ないので、無ければ CSV の最後の行（電流・速度が中止値に近い軸）から推定し、推定と明記する。

ユーザーは「多少飛ばしても先へ進みたい」。判定4（R3 で立てた）なら、次は vx 0.1〜0.2 の床上歩行（吊り紐つき）。速度中止 100°/s と slew（上限 60°/s）の扱いを R3 のログから決める。

## 状況

- 送信器 build `D10_11_LEANSWEEP_20260923_1700`（`.bak_20260923_leansweep`、テスト 150本 OK）。追加は3つ。
  - `--stand-knee-deg K`: 初期姿勢を HFE −K/2・KFE +K・FFE −K/2 にする。
  - `--stand-lean-deg L`: 両 FFE に +L（つま先を下げる向き＝床の上で前へ傾く）。
  - `--lean-sweep-deg M`（`--floor-limits --stand-only` 専用）: 保持のあと 0.5°/s で前傾を足し、FFE の位置のずれ（目標 − 実際）が + から − に変わる角度を `BALANCE LEAN` として出す。`--analyze` も同じ表を出す。
- **D10-10:** 床に降ろすと必ず後ろへ倒れる（足首はつま先が上がる向きに 5〜9°押される。R2 の傾きは後ろへ 14〜20°）。方策の R2 は 2 回とも 0.3 s・2.0 s で停止した。FFE 5.3〜5.96 A だったので、FFE 6 A の電流中止と推定（画面ログ未確認）。URDF の重心は足首より 1.5 cm 前（膝を伸ばしても 1.0 cm）なので、実機の重心がモデルより後ろにある、が作業仮説。
- **AK80-9（HFE・FFE）の CSV の電流の符号は信用しない**（誤差の符号と一致しない）。判定は位置のずれで行う。
- ねじの緩み: 0x11・0x1B・0x2B（本人報告、締め直し済みと想定）。D10-9 の足の接触角は緩んだ状態で測った値。
- 電源の電流はまだ一度も報告がない。
- 学習側への持ち帰り候補: 実機の重心のずれ（`BALANCE LEAN` から推定）を URDF の質量に入れるか、重心のランダム化を足す（`rough_env_cfg.py` には質量の ×0.9〜1.1 と base の ±1 kg しか無く、重心の位置はランダム化していない）。

## 許可／禁止

手順書の「許可／禁止」に従う。R1〜R3 は要承認。吊り紐なし、vx ≠ 0、`--floor-limits` の引き上げは禁止。

## 注意（Cowork）

VM の git は `.git` 内のファイルを消せないので、commit できない。`git --no-optional-locks status` を使い、commit と push のコマンドをユーザーに出す。VM で `--analyze` するときは `pip install --user python-can`。URDF の順運動学・重心は `robot_sim.urdf` の `<axis>`（`0 0 -1` がある）と `<inertial>` を使う。

## 追記（2026-09-23）: D10-11 は未実行のまま、D10-12 を先に行う

**最新はこの追記。** ユーザーに時間がないため、D10-11 は実行せずに残し、D10-11 の測定と方策 run を1回にまとめた **D10-12** を作った。**次のチャットに貼られるのは D10-12 の結果**（D10-11 の結果ではない）。

- 手順: `../../procedures/d10_12_自動前傾_床上方策_実験手順.md`（R0 送信なし、R1 `--floor-limits` で vx 0、R2 `--walk-limits` で vx 0、R3 `--walk-limits` で vx 0.2）。
- 送信器 build `D10_12_AUTOLEAN_WALK_20260923_1730`（`.bak_20260923_autolean`、テスト 155本 OK）。追加は2つ。
  - `--auto-lean-deg M`: 保持の 12 s 目から、FFE の位置のずれ（目標 − 実際、+ ＝かかと）に 0.2°/s/° で追従して前傾を足す。最大 0.5°/s、範囲 0〜M。`AUTO LEAN RESULT` の姿勢から方策を始める。
  - `--walk-limits`: KFE・FFE 10 A、速度中止 200°/s、slew 200°/s まで。要承認。`DualBus.speed_abort_rad_s` でその run だけ変わる。
- 判定は D10-12 の手順書の表で行う。`AUTO LEAN RESULT` は画面にしか出ない。無ければ CSV の stand-hold の FFE の `requested_target_rad` の最後の値 −（−10°）で読む。
- D10-12 で前傾が取れない（`LIMIT REACHED`）か、判断に迷うときは D10-11 の R1・R2（前傾スイープ）に戻る。
- D10-12 で前へ進めたら（判定5）、次は duration と指令を段階的に上げる床上歩行の手順。実機の重心のずれ（前傾の値）は学習側へ持ち帰る。
