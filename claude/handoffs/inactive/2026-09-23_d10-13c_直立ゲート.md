# 引き継ぎ: D10-13C（直立を確かめてから方策を始める。1 ms タイマー・ループ周期の記録）

> 置き換え済み（2026-09-23）。後継は `../active/2026-09-23_d10-13d_原点補正と方策を止めない.md`、結果は `../../reports/2026-09-23_d10-13c_result_トルクと基準姿勢.md`。

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-13B の判定、シムと実機の点検、D10-13C の実装（`../../reports/2026-09-23_d10-13b_result_シムと実機の点検.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_13c_直立ゲート_実験手順.md` の結果を持ってくる**（R0 T265 の傾き、R1 vx 0 を 10 s、R2 vx 0.2、R3 vx 0.4）。

- **自分でユーザーの PC で解析する。** ユーザーの解析出力は信用しない。Cowork では `C:\Users\harut\Connect2USB2CAN` をフォルダアクセスで繋ぎ、VM で `pip install --user python-can` のうえ `python3 ver9_d8_sender.py --analyze logs/<CSV>`。
- 1 run あたりのファイル: `<名前>.csv`（全10軸）、`_obs.csv`（方策の観測と行動）、`_abort.txt`（中止の理由とその場保持の結果）、`_timing.csv`（ループ周期・T265 の経過時間・pitch/roll。保持・ゲート・方策）。
- 方策が何秒続いたかは CSV の `stage=policy` の最初と最後の tick の差で見る。**本人の「立てた」は保持や手の支えの話であることがある。**
- 足裏が平らかは、観測の HFE＋KFE＋FFE（pos_rel の和）と pitch を比べる（報告書の節）。原点に頼らない点検になる。
- ユーザーは時間がなく、「本質的な実験を先に、無理なら戻る」方針。判定に従って、次の実験の実装・手順化まで一気に進める。

## 分岐（手順書の判定表）

- 10 s 続いた → R2・R3 → D10-14（長く・速く・手を外す）→ D10-15 → D11。
- ゲートが開かない → T265 の取り付けの傾きを補正する実装。
- 直立から始めても後ろへ倒れる → D10-12（自動前傾）にゲートを足す。
- 400°/s・原点 45°・電流の中止 → 上げない。別に承認。シムの歩行は膝 450°/s・FFE 最大 12 N·m を使うので、いずれ上げる話になる。
- つま先どうしの接触 → HR の向きか学習側の足幅。

## 状況

- 送信器 build `D10_13C_STARTGATE_20260923_2130`（`.bak_20260923_startgate`、テスト 173本 OK）。`--start-gate-deg`（2〜10）、`--fine-timer`、`_timing.csv`、CSV 名の自動変更、`_abort.txt` にその場保持の結果。前 build の `--walk-speed-abort-dps`（≤400）・`--soft-stop-seconds`（≤5）はそのまま。
- **D10-13B のまとめ:** 直立で始まった 1 回だけ、方策は 7.4 s 足踏みして釣り合った（HAA −10〜−21°まで閉じた。つま先の接触の可能性）。後ろへ 14〜26°傾いて始まった run は 0.7 s 以内に原点中止（LR_HFE −47〜−49°）か Ctrl-C。
- **点検で合っていたもの:** 関節角速度（位置の差分と倍率 0.94〜1.06）、T265 の軸・符号（重力の変化と −ω×g が倍率 0.96）、Kp/Kd の換算。**差があったもの:** ループ周期（0/16/31/47 ms の刻み）、開始状態、中止値とシムの歩行の範囲、摩擦。
- AK80-9（HFE・FFE）の CSV の電流の符号は信用しない。
- 電源の電流はまだ一度も報告がない。

## 注意（Cowork）

- VM の git は `.git` 内のファイルを消せないので commit できない。`git --no-optional-locks` を使い、commit と push のコマンドをユーザーに出す。
- commit は `git commit -m ... -- <パス>` の形で、自分の変更だけにする。
