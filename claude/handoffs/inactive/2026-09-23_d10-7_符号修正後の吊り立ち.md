# 引き継ぎ: D10-7（符号を直した送信器で、吊ったまま立たせ、方策を回す）

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-6 の判定、符号の実装、D10-7 の実装（`inactive/2026-09-23_d10-6_関節符号確認.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_7_符号修正後_吊り立ちと方策_実験手順.md` の R0〜R4 の画面ログ、CSV 名、R1 の写真と所見、電源の電流を貼る。** 保存した CSV を `--analyze` に掛け、手順書の判定表で判定する。**R1 で全関節が鏡像になったことを確認するまでは、方策の run（R3・R4）を許可しない。**

## 状況

- 実機→H の符号は、LL_HAA・LR_HR・LR_HFE・LR_KFE・LL_FFE の5軸が −1（D10-6）。正本は `robot_joint_map.JointBinding.sign`。
- 送信器 build `D10_7_SIGNPOSE_20260923_1430`（テスト 129本 OK）。
  - `--sign-pose`: シムの初期姿勢に、両脚とも HR・HAA を +8° 足して保持する。`--stand-only` が必須。
  - 起動時に `JOINT SIGNS` の行を出す。
  - 保持が終わると `LOOK CHECK` を出す。`--analyze` は保持した姿勢を CSV から読む。
  - D7 の `scan` は、全軸の角度を実機の座標とシムの座標（H）の両方でログに書く。
- **符号が正しいかは観測値では判定できない**（位置ループは渡された座標系の中で閉じるので、観測値は常に目標どおりに見える）。判定の根拠は R1 の写真と見た目である。
- D10-3〜D10-5 の CSV は、符号が誤ったまま記録されている。
- 電源: JESVERTY 2台（合計 20 A）。片脚に1台ずつつなぎ、GND を共通にする。各 run で電源の電流表示を記録してもらう。今までの電流の値は相電流なので、電源から流れる電流は実測で確かめる。
- 保留中の提案: `--lock-haa`（吊りの診断専用）。符号を直してもまだ足同士がぶつかるときだけ実装する。

## 許可／禁止

手順書の「許可／禁止」に従う。R1〜R4 は要承認。床上の run は禁止。

## 注意（Cowork）

VM の git は `.git` 内のファイルを消せず、作成者名も未設定なので、commit できない。**`git status` も `.git/index.lock` を残す。** 状態を見るときは `git --no-optional-locks status` を使う。commit と push は、ユーザーの PC で行ってもらう（コマンドを毎回出す）。
