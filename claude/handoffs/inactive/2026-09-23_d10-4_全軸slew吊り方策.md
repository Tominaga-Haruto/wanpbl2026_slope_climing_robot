# 引き継ぎ: D10-4（手を離した吊り保持の取り直し → 全軸 slew つき吊り方策）の結果を判定する

> **完了（2026-09-23）・後継は `../active/2026-09-23_d10-5_基準姿勢から方策.md`。** 結果 `../../reports/2026-09-23_d10-4_result.md`。

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-3 の判定と送信器修正（`../../reports/2026-09-23_d10-3_result.md`、完了）

## 最初の仕事

**ユーザーが `../../procedures/d10_4_全軸slew_吊り方策_実験手順.md` の画面ログを貼る。CSV は `C:\Users\harut\Connect2USB2CAN\logs\d10_4_*` を自分で `--analyze` に掛け、手順書の判定表（1〜10）で切り分ける。** Cowork の場合は `C:\Users\harut\Connect2USB2CAN` をフォルダ接続し、VM に `pip install python-can` すれば `--analyze` が回る（numpy・onnxruntime は既にある）。

## 状況（これだけ分かっていればよい）

- **D10-3 R1・R2（`--hold-pose`）は 1.0 A のまま完走、保持 ≤0.10 A。ただし脚を手で支えていた。** D10-4 R1 は同じコマンドを手を離して回し、これを確定させる。
- **D10-3 R3/R4 は ramp を完走し、方策切替の 1〜2 tick 目に電流中止。** slew が 0x1C だけに掛かり、残り9軸が 20 ms で最大 45° 跳んだため（R3: 0x22 4.30 A、R4: 0x1A 4.35 A）。**方策そのものや中止値のせいではない。**
- ramp 中の電流は「遅れ × Kp」。LL_HAA が 8° 遅れて 1.7〜2.0 A、LL_HFE 0.9〜1.2 A（手の支えあり）。**D10-2 の「1.0 A が自重より小さい」は「脚を初期姿勢へ動かすのに 1.0 A を超える」に訂正済み。**
- 吊りの初期方策姿勢は「左脚が後ろ・右脚が前に交差」（両 HAA −13〜−23°、LR_HR −13°）。接地のない観測は学習分布の外なので、吊りの方策出力は歩容として読まない。
- **slew つき run は歩容の再現ではない。** 見るのは「全身ループが中止なしで回るか」と軸ごとの電流。

## 送信器（build `D10_4_ALLSLEW_20260923_1200`）

`C:\Users\harut\Connect2USB2CAN\ver9_d8_sender.py`（`.bak_20260923_allslew`）。テスト 83本 OK。

- `--policy-slew-dps D`（0<D≤60）: 方策段階の全10軸を D °/s に制限。`--arm --all-axes` の方策 run で必須。1軸 run・hold-pose は不変。
- 全軸プレアーム・ゲート: |初期目標| > 40° か |delta| > 30° の軸があれば MIT 送信前に拒否。preflight でも表示。
- 停止時（中止でも）に `POLICY STAGE: N tick(s)` と `SLEW GAP`。`--analyze` も出す。`--analyze` の asked は ramp の固定目標に修正。

## 分岐（詳細は手順書の判定表）

- R1 完走 → R2（slew 20、vx 0）→ 完走なら R3（slew 30、vx 0.2）。
- R1 中止 → R1b（`--gravity-limits` で hold）。
- R2 が方策段階で中止 → **slew も上限も上げない。** `SLEW GAP` を見て報告。
- R3 で左右交互 → 床上は**別手順を書いてから**。中止値は実測電流から決める。

## 許可／禁止

- **許可:** R0・R1・`scan`・`oa`・`--preflight`・`--analyze`・文書更新。
- **要承認:** R1b・R2・R3。
- **禁止:** run 中に脚を手で支えること。Kp/Kd・トルク欄の増加。速度中止・stale・原点 45° の緩和。`--gravity-limits` の表の引き上げ。slew 30 °/s 超・vx 0.2 超・duration 5 s 超。同名 CSV の上書き。床上の run。
- ユーザーが「無視して」と名付けた CSV（`...korehamusisite,zettainiminaide.csv`）は判定に使わない。

## 並行（実機不要）

- 実験12（今夜の学習）・実験11（friction スイープ）。
- 立たせた基準姿勢で D7 原点を取る支持治具（床上の前提条件）。
- HR 可動域 外90°／内45° を URDF・シムへ。

## ユーザーの要望

時間が無い。飛ばし気味でよいが、吊り→床の順と「測ってから値を決める」は崩さない。頼まれていない文書を増やさない。commit/push はエージェントが行う。結果は申告ではなく保存ログで判定する。

## 記録

CSV・生ログは Git に入れない。要約と絶対パスを `../../reports/` に残し、`../../CONTEXT.md` と次の引き継ぎを同じ commit で更新する。
