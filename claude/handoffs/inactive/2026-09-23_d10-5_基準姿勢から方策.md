# 引き継ぎ: D10-5（シムの初期姿勢へ移して保持 → slew つき方策、吊り）の結果を判定する

> **完了（2026-09-23）・後継は `../active/2026-09-23_d10-6_関節符号確認.md`。** 結果 `../../reports/2026-09-23_d10-5_result.md`。

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-4 の判定と `--stand-seconds` の実装（`../../reports/2026-09-23_d10-4_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_5_基準姿勢から方策_実験手順.md` の画面ログを貼る。CSV は `C:\Users\harut\Connect2USB2CAN\logs\d10_5_*` を自分で `--analyze` に掛け（stand-hold の表も出る）、判定表（1〜7）で切り分ける。** Cowork なら `C:\Users\harut\Connect2USB2CAN` をフォルダ接続し、VM に `pip install python-can`。

## 状況

- **D10-4 R2・R3 は全軸 slew で方策段階を完走（初の全身ループ）。** ただし (a) 途中から D7 `oa` を脚を閉じた姿勢で打っていた（HAA 原点が左約6°・右約12° ずれ、推定）、(b) 全軸 0°（膝が伸びた姿勢）から始めていて、方策は KFE +178° などを要求していた。本人所見: 左つま先と右かかとがぶつかって止まった。
- **`oa` の運用（本人の指摘で確定）:** 基準姿勢で持ったまま `oa` → `scan 3` で 0.0° → run の最初の `RAMP progress` まで持ち続けて、そこで離す。手を離す必要はない。
- 吊り静止の保持（脚が閉じ切った位置）は ≤0.05 A。**脚を開いた初期姿勢の保持電流はまだ測っていない**（D10-5 R1 で測る）。これが床上中止値の根拠になる。
- 吊りは接地がないので、初期姿勢から始めても方策は分布の外のまま。歩容は床でしか見えない。吊りで確かめるのは、初期姿勢の保持・切替・足同士の干渉まで。

## 送信器（build `D10_5_STANDPOSE_20260923_1300`、`.bak_20260923_standpose`、テスト 89本 OK）

- `--stand-seconds S`（0<S≤5、`--all-axes` のみ）: シムの初期姿勢（`DEFAULT_JOINT_POS`: HR 0 / HAA 0 / HFE −10 / KFE +20 / FFE −10°）へ `--ramp-seconds` で移し（stage `stand-ramp`）、S 秒保持（`stand-hold`、`HOLD POSE RESULT` を表示）、前回行動ゼロ・初期姿勢から全軸 slew で方策へ。ゲートは初期姿勢までの delta で判定。
- `--stand-only`: stand-hold で終わる（方策・`--duration`・slew・速度指令なし）。
- 既存の `--policy-slew-dps`・プレアーム・ゲート・`SLEW GAP` はそのまま。1軸 run・`--hold-pose` は不変。

## 許可／禁止

- **許可:** R0・`scan`・`oa`・`--preflight`・`--analyze`・文書更新。
- **要承認:** R1・R2・R3。
- **禁止:** stand-hold・policy 中に脚を触ること。脚を閉じた姿勢での `oa`。Kp/Kd・トルク欄の増加。速度中止・stale・原点 45° の緩和。`--gravity-limits` の引き上げ。slew 30 超・vx 0.2 超・duration 5 s 超。同名 CSV の上書き。床上の run（R3 で左右交互が出たら床上手順を別に書く）。

## 並行（実機不要）

実験12・実験11、HR 可動域を URDF へ。支持治具は「基準姿勢で持ったまま `oa`」で当面代替できる。

## ユーザーの要望

時間が無い。飛ばし気味でよいが、吊り→床の順と「測ってから値を決める」は崩さない。頼まれていない文書を増やさない。commit はエージェント、push はユーザーのPCから（このVMは GitHub 認証なし）。結果は保存ログで判定する。
