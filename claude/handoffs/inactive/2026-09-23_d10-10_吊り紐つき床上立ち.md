# 引き継ぎ: D10-10（吊り紐を付けたまま、足を床に着けて立たせる）

> 完了・置き換え済み（2026-09-23）。後継は `../active/2026-09-23_d10-11_床上前傾スイープ.md`。

作成 2026-09-23 / 対象: 次のチャット（実機側）/ 前任: D10-9 の判定と D10-10 の実装（`../../reports/2026-09-23_d10-9_result.md`）

## 最初の仕事

**ユーザーが `../../procedures/d10_10_吊り紐つき床上立ち_実験手順.md` の R0〜R2 の結果を貼る。** 画面ログ、`--analyze` の出力、CSV 名、吊りを下ろし終えた時刻、所見、動画、電源の電流がそろっているはず。**ユーザーは `--analyze` を打ち忘れることが多い。** ユーザーの PC で必ず自分で掛け、手順書の判定表で判定する。

## 状況

- 送信器 build `D10_10_FLOORSTAND_20260923_1600`（`.bak_20260923_floorstand`、テスト 143本 OK）。`--floor-limits`: `--gravity-limits` の表で KFE・FFE だけ 6.0 A（URDF の両足立ちの静的推定 KFE ≤3.9 A、FFE ≤4.7 A の約1.5倍）。このときだけ `--stand-seconds` ≤30 s。5 s を超える保持では 2 s ごとに `STAND HOLD t/T s: max|I| ...` を表示。
- **R1 の `HOLD POSE RESULT`（保持の最後 25%）が、床上の荷重時保持電流の初の実測になる。** 床上の中止値の表はこれから作る（推定で決めない）。
- **D10-9:** 足は HAA 約 −8.5°で触れ始め、−10.3 / −11.0°で止まる（R1・R2 で同じ、原点は安定）。当たるのはつま先だけ（左右のつま先が平行でない）。本人の判断で許容して進んだ。シムの歩行は HAA −13.5〜−16.4°（足首間隔 18 cm）なので、床上でつま先が触れる可能性はある。
- 歩かせる（vx > 0）には、速度中止 100°/s（シムの KFE 速度の標準偏差は 250°/s）と slew（上限 60°/s）の扱いを決める必要がある。R2 のログから決める。
- 電源の電流はまだ一度も報告がない。床上では必須と手順書に書いた。

## 許可／禁止

手順書の「許可／禁止」に従う。R1・R2 は要承認。吊り紐なしの床上、vx ≠ 0 は禁止。

## 注意（Cowork）

VM の git は `.git` 内のファイルを消せないので、commit できない。`git --no-optional-locks status` を使い、commit と push のコマンドをユーザーに出す。VM で `--analyze` するときは `pip install --user python-can`。URDF の順運動学は `<axis xyz="0 0 -1">` を必ず入れる（`~/work/fk.py` は VM の一時領域なので無い前提で書き直す）。
