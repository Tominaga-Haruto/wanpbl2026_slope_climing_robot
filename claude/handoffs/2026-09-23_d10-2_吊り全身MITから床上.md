# 引き継ぎ: D10-2（吊り全身MIT → 床上）の結果を切り分ける

作成 2026-09-22 / 対象: 次のチャット（実機側）/ 前任: D9-6（`../reports/2026-09-22_d9-6_result.md`、完了）

## この引き継ぎの最初の仕事

**ユーザーが `../procedures/d10_2_吊り全身MITから床上_実験手順.md` の R0〜R2 を回した結果（CSV・画面ログ・動画）を貼る。それを同手順書の判定表（1〜8）で切り分け、次の一手にする。**

最初の依頼文（そのまま使える）:

> D10-2 の R0〜R2 の結果を貼ります。`procedures/d10_2_吊り全身MITから床上_実験手順.md` の判定表で切り分けて、次の一手にしてください。時間が無いので飛ばし気味で。ゲイン・トルク欄・電流中止・速度中止は上げないでください。

## 状況（これだけ分かっていればよい）

- **D9-6 で3軸が判定 A（動く）。0x1C は故障していない。** 0x1C 0.90 A/6.50°、0x13 0.63 A/4.55°、0x2B 0.72 A/2.17°。MITトルク経路は3本とも verified。
- **ユーザーの判断で片脚MITと残り7軸の1軸 probe を飛ばした。** 時間制約による。飛ばしたぶんのリスクは「未測定7軸のどれかが 0x1C 並みに重い」で、R1 で**その軸だけ目標に届かない**形で出る。判定表の 4 で拾う。
- 送信器 build **`D10_ALLAXES_20260923_0100`**。`--all-axes` を追加した（全10軸へ送る。`--motor-id` との併用は拒否。`ALL AXES:` の1行が出る）。テスト 58本 OK。`.bak_20260922_allaxes` あり。
- **既存の中止は全部生きていて、全10軸を見ている。** 電流 1.0 A、速度 100°/s、stale feedback 0.3 s、原点45°。**これらとゲイン・トルク欄は上げない。**
- **0x2A（LR_HFE）に注意。** 2026-09-21 に `stale feedback 0x2A` で止まった実績がある。判定表の 2。
- 方策は **H_eff13p5@2999**、パッケージは `C:\Users\harut\wanpbl2026_slope_climing_robot\H_eff13p5_2999\H_eff13p5_2999`。
- **D7 の原点は 2026-09-22 夜に手でだいたいの初期姿勢に合わせて取り直したもの。** R0 の `Initial policy targets` で `delta` が ±30° を超える軸があれば、原点がずれているので実行しない。
- **HR の機構的可動域は 外 約90° / 内 約45°（配線長）。** URDF は ±π のまま。方策が内側 45° を超える HR を出すと配線を引っ張る。R2 以降で HR の目標角を見る価値がある。

## 分岐（詳細は手順書の判定表）

- **1**（R1 完了）→ R2。同じ通電中でよい。
- **2**（stale feedback）→ 主電源OFF。`procedures/d9_受信確認_実行シート.md` の送信なし確認へ戻る。
- **3**（current / speed abort）→ 主電源OFF。中止行のIDを1軸 probe に戻す。**値は上げない。**
- **4**（特定の軸だけ届かない）→ その軸を1軸 probe。床へ下ろさない。
- **5**（R2 で左右交互）→ ユーザー確認のうえ **R3（床上・要承認）**。
- **6**（R2 で片側だけ／震える）→ 床へ下ろさない。実験11 の結果と突き合わせる。
- **7**（R3 で崩れる）→ R4 へ行かない。実験11 P3（stiffness 見直し）をユーザー判断に上げる。
- **8**（R4 で歩く）→ 床上デプロイ成功。報告書へ。

## 許可／禁止

- **許可（承認不要）:** R0・R1・R2（吊り）。`scan`・`--preflight`・`--analyze`・文書更新。
- **要ユーザー承認:** **R3・R4（床上）。** 未測定7軸の1軸 probe。
- **禁止:** Kp/Kd・トルク欄・電流中止（1.0 A）・速度中止（100°/s）の増加。`--duration` 5秒超。走行中に手で機体を支えること。同じCSV名で上書きすること。R1 を飛ばして R3 へ行くこと。

## 並行（実機・GPU 不要）

- **実験11（WRS friction スイープ）** `../wrs_experiment11_friction_instruction.md`。実機の不感帯はシムの 1.7〜3.1 倍。判定 6・7 に当たったとき、この結果が無いと次が決まらない。
- HR の可動域 外90°／内45° を URDF・シムへ入れる。
- 立たせた基準姿勢で D7 の原点を取り直すための支持治具。

## 進め方についてのユーザーの要望（2026-09-22）

- **時間が無い。段階を飛ばしてよい。** ただし吊り→床上の順と、R1 を先に通すことは崩さない。
- **頼まれていない文書を増やさない。**
- **commit と push は Claude が行う。** シェルが使えないセッションでは、実行フォルダを埋めた完成形のコマンドを毎回出す（`AGENTS.md` の「commit と push」節）。
- **結果は本人の申告ではなく保存ログで判定する。** 「動いたと思う」は所見であって測定値ではない。**CSV名は実行ごとに変える**（2026-09-22 に固定名で前の回を消した）。

## 記録

CSV・生ログ・重み・鍵はGitに入れない。要約と絶対パスを `../reports/` に残す。作業後は `../CONTEXT.md` と次の引き継ぎ書を同じcommitで更新する。
