# D10-13 結果: 方策の段階はどの run も 0.2〜4.3 s で中止した（推定: 速度中止 200°/s）。膝から崩れたのは中止のあとのトルク 0 の指令による

作成 2026-09-23。送信器 build `D10_13_FLOORWALK_20260923_1800`。CSV は `C:\Users\harut\Connect2USB2CAN\logs\d10_13_*.csv`（2026-09-23 18:14〜18:36 JST）。判定には、Cowork から掛け直した `--analyze` と、CSV・観測 CSV の生の値を使った。本人が掛けた `--analyze` は本人の申告で信用しない。画面ログは貼られていないので、中止の理由は推定である。

## 結論（3行）

1. **方策の段階は全 8 run で中止して終わった。** R1（5 s の予定）は 0.31・0.31・0.34 s、R2 は 0.99 s（vx 0.2 に切り替わる前）、床の上の R3 は 0.16・0.19 s。吊り上げた R3 だけが 4.25 s・2.66 s 続いた。
2. **中止の理由は速度中止 200°/s と推定する。** 最後に記録された tick で、どこかの軸が 186〜211°/s でまだ加速していた。電流は最大 8.7 A（FFE）で 10 A に届かない。R3 ver4 だけは LR_HFE が −45°で、原点 45°の中止の可能性もある。シムの方策は膝を約 250°/s（標準偏差）で動かすので、200°/s は方策がふだん使う速さより低い。
3. **「膝から崩れ落ちた」のは、中止のあとの cleanup（全軸 Kp=Kd=0 の MIT）で力が抜けたため。** 方策そのものの動きの結果ではない。

## 各 run

| CSV | 方策の段階 | 止まる直前（最後の tick） | 本人所見 |
|---|---|---|---|
| `d10_13_r1_stand_zero_a.csv` | 0.31 s（16 tick） | LL_FFE 186°/s、LR_FFE 179°/s | 静止できた。前へ押しても足を出さず、トルクでその姿勢に止まる |
| `d10_13_r1_stand_zero_a_ver2.csv` | 0.31 s | LL_HFE 194〜199°/s | 同上 |
| `d10_13_r1_stand_zero_a_ver3.csv` | 0.34 s | LR_HFE 190°/s、LL_FFE 186°/s | 同上 |
| `d10_13_r2_walk_vx02_a_ver2.csv` | 0.99 s（指令は 0 のまま） | LL_KFE 167→188°/s | 歩こうとすると膝から崩れ落ちた |
| `d10_13_r3_walk_vx04_a.csv` | 4.25 s（vx 0.4 は 2.25 s） | LL_KFE 197°/s | 吊り上げて、ばたばた歩こうとした |
| `d10_13_r3_walk_vx04_a_ver2.csv` | 0.16 s | LR_HFE 190°/s | 膝から崩れ落ちた |
| `d10_13_r3_walk_vx04_a_ver3.csv` | 0.19 s | LR_HFE 211°/s | 同上 |
| `d10_13_r3_walk_vx04_a_ver4.csv` | 2.66 s | LR_HFE −45°、LL_FFE 193°/s | 吊り上げて、ばたばた |

R0 と `r2_..._a`（ver なし）の CSV は無い。電源の電流は今回も報告なし。

## 方策が始まる時点と、最初の 0.3 s（R1 の 3 回で同じ）

- 保持の最後（方策の直前）の観測: 体は約 4〜5°傾いている（gravity x −0.07〜−0.09）。足首はつま先が上がる向きに 8〜9°押されている（pos_rel FFE −8 / −9°）。D10-10 と同じ、かかとに乗った状態である。
- 方策の最初の行動: FFE +1.0（0.5 倍で +29°、つま先を下げる）、HFE +0.1〜0.6、KFE −0.7。
- 0.1 s で胴の ang_vel y が 1.0〜1.4 rad/s に上がる。0.3 s で股・足首が 190°/s 前後に達して中止。
- SLEW GAP: LL_FFE が方策の要求より最大 206°遅れていた（slew 200°/s）。

## 本人所見とログの食い違い

本人は「R1 は方策の段階で静止できた」と答えた。ログでは方策の段階は 0.3 s で終わっており、送信器のコードも「duration が尽きるか例外」でしか方策の段階を抜けない（`ver9_d8_sender.py` の方策ループ）。可能性は2つある（どちらも推定）。

- 押したのは 15 s の保持（送信器の固定姿勢、stand-hold のゲイン）の最中だった。
- 中止後に力が抜けたあとも、減速機の摩擦（実機はシムの 1.7〜3.1 倍）と吊り紐で、姿勢が崩れずに止まっていた。

次の build は中止の理由と時刻を `<CSV名>_abort.txt` に残す。これで食い違いを判定できる。

## 判定と次

手順書の判定 **3（中止）** に当たる。ユーザーが次を選んだ: **速度中止を上げ（最大 400°/s）、中止のあとに力を抜かずにその場で保持してから止める。** → D10-13B（`../procedures/d10_13b_速度中止400_中止後保持_実験手順.md`、build `D10_13B_SOFTSTOP_20260923_1900`）。
