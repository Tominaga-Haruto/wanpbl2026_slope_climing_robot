# D10-4 結果: 全軸 slew で吊り方策ループが初めて完走。ただし原点を脚を閉じた姿勢で取っており、方策は吊りで破綻

> 2026-09-23 12:09〜12:20（JST）。送信器 build `D10_4_ALLSLEW_20260923_1200`。判定は保存CSVの `--analyze` と本人所見。
> 手順 `../procedures/d10_4_全軸slew_吊り方策_実験手順.md`。後継 `../procedures/d10_5_基準姿勢から方策_実験手順.md`。

## 結論（3行）

1. **R2（slew 20 °/s、vx 0）と R3（slew 30 °/s、vx 0.2）は中止なしで方策段階を完走（136 / 138 tick）。** 全身の方策ループがつながったのは初めて（判定4）。
2. **R2 の途中から、D7 `oa` を脚を閉じた姿勢で打っていた。** 方策は閉じた脚を「HAA 0＝まっすぐ」と読む。D10-3 のログでは、まっすぐで `oa` した後に脚が閉じると LL_HAA +4.5〜+5.9°、LR_HAA −9.5〜−12.5° になるので、**原点のずれは左約6°・右約12°**（推定）。R2・R3 で「左つま先と右かかとがぶつかって止まった」のはこれが一因と考える。
3. **吊りの方策出力は学習分布の外で、要求は KFE 最大 +178°、HFE −71°、FFE −100°。** 全軸 0°（膝が伸びた姿勢）から始めたこと自体が分布の外（シムは初期姿勢 ×0.5〜1.5 でリセット、KFE 10〜30°）。→ D10-5 で「シムの初期姿勢へ ramp → 保持 → 方策」に変える。

## run

| run | CSV（`C:\Users\harut\Connect2USB2CAN\logs\`） | 結果 |
|---|---|---|
| R0 | `d10_4_r0_preflight_a.csv` | 送信ゼロ |
| R1 | `d10_4_r1_hold_handsoff_a.csv` | 3 s 完走。全軸 ≤0.05 A、垂れ 0°。**脚は閉じ切って止まった位置なので重力はほぼ掛かっていない** |
| R1b | `d10_4_r1b_hold_handsoff_gravity_a.csv` | 同上（動かないのが正常） |
| R2（1回目） | — | **プレアーム・ゲートが拒否**: `0x1B LR_HAA target=-30.7deg delta=-31.9deg`。MIT 送信なし。以降は脚を閉じたまま `oa` |
| R2 | `d10_4_r2_hang_zero_slew20_a.csv` | 完走。最大電流 LR_HFE 3.20 A、LL_HFE 2.69 A、LR_HAA 1.76 A |
| R3 | `d10_4_r3_hang_vx02_slew30_a.csv` | 完走。最大電流 LR_HFE 4.40 A、LL_HFE 3.15 A、LR_HAA 1.81 A |

- 方策の要求（R3）: KFE −66〜+178°、HFE −71〜−13°、FFE −100〜+51°。SLEW GAP 最大 178°（LR_KFE）で、全軸 80〜100% の tick で slew に抑えられていた。送った目標は ±45° 以内に収まっている。
- 本人所見（R2）: 右足を前・左足を後ろにした後、前後を入れ替えようとし、前に出す左つま先と後ろに引く右かかとがぶつかって止まった。R3 もほぼ同じ。左右の足が触れ合いながら前後に開こうとしていた。

## D7 `oa` の運用（本人の指摘で確定）

`oa` は打った瞬間の姿勢を関節角 0° にするだけ。**基準姿勢（脚をまっすぐ下・左右平行）で持ったまま `oa` → `scan 3` で 0.0° を確認し、run の ramp が始まるまで持っていてよい。** 手を離す必要はない（本人の過去の運用でも `scan 3` で 0.1° も変わらなかった）。
