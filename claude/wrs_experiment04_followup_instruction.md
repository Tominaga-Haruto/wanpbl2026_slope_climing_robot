# 指示書 実験04 停止点1 の講評と追加作業 P5（WRS機）

> 作成 2026-09-17 朝。実験04 停止点1（2026-09-17 00:09）の報告を受けて。実験04 と同じ WRS チャットに続けて貼る。
> 読みの根拠・判断は `chats/2026-09-17_exp04-stop1-review.md`。
> GPU 占有の終わりは 13:00 と仮置き。ユーザーの了承と違えば 1 行直して貼る。

```
# 指示: 実験04 停止点1 の講評と追加作業（停止点2 は予定どおり ＋ P5）

停止点1 の報告ありがとう。import onnx の順番の件と報告書の直し3点は助かった。
前の指示（wrs_training_operator_instruction.md の掟、実験03 の「更新」、実験04 の「やらないこと」）はすべて有効。起動は _launch.ps1 / _eval.ps1 / _play.ps1。
GPU の占有はユーザーが 2026-09-17 13:00 まで了承。開始時に nvidia-smi で他人の計算プロセスがあれば、何も起動せず報告して止まる。

## 0. P4 の2本目について
選択は指示の規則どおりで正しい。規則の作り方がまずかった: 条件4（effort 9.0）は定格であって、Kt の誤差の想定（0.7 倍 ≒ 12.6 → 条件3）ではない。条件2・3 は両 iter で合格なので H_eff13p5 の保険は小さい。
H_eff13p5 は止めずに最後まで回して、予定どおり評価する。ゲイン系の保険は P5-5 で足す。

## 1. 停止点1 の所見の訂正2つ（出し直しは P5-7）
(a) 左右 4 cm のずれは「半分」ではなく、全部 base 原点の横ずれで説明できるはず。股の中点を c = -0.0206 m とすると、左 y = c + d、右 y = c - d を y=0 で鏡映して比べた差は 2c = -0.041 m で、P1-4 の -0.038〜-0.040 と一致する。
(b) 「足裏の pitch -40°」はリンク座標系の姿勢であって、足裏の平面の傾きとは限らない。

## 停止点2（予定どおり）
前の指示どおり報告する。追加: H_nolinvel@2999 の頑健性は条件 1・3・10・15 に 6・8・16 を足して計7条件。
停止点2 の報告を出したら、止まらずに P5 へ進んでよい。

## P5 追加（P4 の学習が終わってから。64 env の評価は並行してよい。env 構築が 10 分進まなければ止めて env を減らす）

### P5-1 P1 の表の欠けを埋める（ログから。足りなければ再実行）
- 条件ごと × 2400 / 2999: S1 の転倒率 [%]・静止率 [%]、S3 の転倒率 [%]、S7 / S8 の yaw rate と判定、HFE・KFE・FFE の飽和率、|computed torque| の p99。
- v_x / |v_y| / yaw rate を「転倒していない env だけ」の平均でも出す（条件9 の v_x 0.93 は転倒中の動きが混ざっている疑い）。
- 各条件の .md 冒頭に「テンソルから読み直した実効値」があるか確認。条件5・12・14 が基準とほぼ同じ数字になった理由（上書きが効いていないのか、効いても変わらないのか）を、その条件の実効値と KFE の最大 applied torque で示す。
- 条件10・11 で実際に入った遅延 step 数と、1 step の ms（physics dt）。
- 吊り下ろし（条件16）の「最初の 2 s の転倒率」。

### P5-2 条件9（damping ×2）の破綻がシムの数値不安定かを確かめる（評価だけ、2999）
DelayedPD は陽的な PD なので、damping × physics_dt / (armature ＋ 関節から見たリンク慣性) が大きいと数値的に発振しうる。実機のモーターは内部で高速に PD を回すので、この発振は実機では起きない。
1. physics dt、各グループの armature・damping から damping×dt/armature を、条件1 と条件9 で表にする。
2. 条件9 の S1 で、HFE・KFE・FFE の関節速度が 1 step ごとに符号反転している割合と関節速度の RMS を、条件1 と並べる。
3. 条件9 を physics dt 半分（sim.dt を 1/2、decimation を 2 倍、遅延の step 数も 2 倍にして、方策の周期 20 ms と遅延の ms は同じ）で回し直す。転倒率が 5% 以下に戻れば「シムの数値不安定」と判定。
4. 条件8（damping ×0.5、S1 yaw -0.13）も 3 と同じ dt 半分で1回。

### P5-3 G_real_peak が base_lin_vel にどれだけ頼っているか（評価だけ、2999）
方策に入る base_lin_vel だけを加工する（物理はそのまま、ほかの観測もそのまま）:
(i) 0 固定、(ii) 真値 ＋ バイアス (+0.1, 0, 0) m/s、(iii) 真値 ＋ ガウス雑音 σ 0.1 m/s、(iv) 5 step（100 ms）ごとにしか更新しない（ゼロ次ホールド）。
S1 / S3 / S7 / S8 で P1 と同じ1行。

### P5-4 実機のデプロイコードの検証用データ（G_real_peak@2999 と H_nolinvel@2999）
- S1 の 1 env を 500 step 回し、1 step ごとに npz に保存: 観測の各項（scale・clip 前の物理値と、scale 後の値の両方、項の名前付き）、方策出力の生の action、関節目標角（既定角 ＋ 0.5×action）、関節角・関節速度、applied / computed torque、指令。
  保存先: tools\\logs\\golden_G_real_peak_2999.npz と tools\\logs\\golden_H_nolinvel_2999.npz。中身の説明を同名の .md に。
- action の clip の有無（コードのファイルと行番号）、S1 全体の |action| の p99 と最大（関節ごと）、関節目標角の範囲と URDF の関節リミットの比較表。
- obs_contract.md の G_real_peak の節に「デプロイ用アクチュエータ表」を追記: 関節順どおり10行で、グループ・stiffness・damping・effort_limit・armature・friction・velocity_limit・遅延 step・既定角 [rad]・URDF リミット [rad]。テンソルから読んだ値で。

### P5-5 保険の学習 H_gainDR（P5-2 の判定後に起動。4096 env、seed 1、3000 iter、一から、1本）
- G_real_peak とまったく同じ起動行 ＋ mdp.randomize_actuator_gains（mode reset、全グループ、stiffness ×0.7〜1.3、damping ×0.5〜1.5）。
- rough_env_cfg.py に「既定は無効（1.0〜1.0）」で足す。.bak_gaindr を取ってから。範囲は起動行の上書きで入れる。
- damping の上限: P5-2 で「数値不安定」なら 1.5 のまま。「数値不安定ではない」なら 2.0 にする。どちらにしたかを報告に書く。
- 64 env / 20 iter で通す → params\\env.yaml で範囲の反映確認 → diff → ハードリンク5組の Get-FileHash とリンク数 → commit（push しない）。
- 評価: iter 1000 / 2000 / 2999 で measure_crab.py（S1〜S9、トルク指標込み）、2999 で P1 の条件 1・3・6・7・8・9・10・16。

### P5-6 ONNX の小さな確認
- exported_2400\\policy.onnx と exported_2999\\policy.onnx の SHA256。同じ実観測 200 個での2つの ONNX の出力どうしの最大差（別物であることの確認）。
- H_nolinvel@2999 と H_eff13p5@2999 も書き出し（exported_2999 のように分けて）、verify_onnx.py で検証。H_nolinvel は入力 39 次元であること。

### P5-7 形状の出し直し（学習 cfg は変えない）
1. 鏡映の軸を y=0 ではなく股の中点 y=-0.0206 にして、左右5ペアのリンク原点・リンク COM の (x, y, z) の差を出し直す。残差が数 mm 以下なら「脚は対称、base 原点が横に 2 cm ずれているだけ」と判定。
2. 足裏の平面: 既定姿勢で、足の衝突メッシュの頂点を world 座標にして、最下点から 3 mm 以内の頂点に平面を当てはめ、その法線から地面に対する pitch / roll [deg]、当てはめに使った頂点数。リンク座標系の姿勢（-39.7° / -40.8°）とは別に出す。
3. その頂点群の x 範囲（base 座標）と、全身 COM の位置（base 座標と、world での足の接地範囲に対する x）。COM の x が接地範囲の前に出ているかを1行で。

## 【停止点3】P5-1〜P5-4・P5-6・P5-7 が終わり、H_gainDR を起動したら報告（学習は回したまま）
- 冒頭3行: 条件9 は数値不安定か、G_real_peak の base_lin_vel への依存の強さ、H_gainDR の damping 上限
- P5-1 の埋めた表、P5-2 の表、P5-3 の表、P5-4 のファイルの場所とアクチュエータ表と action の範囲、P5-6、P5-7
- H_gainDR の起動時刻と終了見込み、commit のハッシュ
推測で直さない。想定外（上書きが効かない、dt 半分で env が作れない、npz の項名が obs_contract と合わない、等）は止まって報告。
13:00 を過ぎそうな学習は起動しない（起動せずに報告）。
```

---

## 追加（2026-09-17 10:40、停止点2・3 を受けて）P6 と停止点4

> 停止点2: H_nolinvel@2999 は S1 v_x 0.315（< 0.35）、S5 横移動で静止率 0.778、stiffness ×0.7 で S1 転倒 100%、遅延 4 step で小旋回が境界不合格 → **不採用**。H_eff13p5@2999 は G_real_peak とほぼ同じ合否で、S1 の FFE RMS 5.97（G 7.04）・computed max 18.0（G 21.4）とトルクが低い → 候補に加える。
> 停止点3: 条件9 は数値不安定ではない（dt 半分でも転倒 98.4%）、G_real_peak は base_lin_vel を 0 にすると壊滅（バイアス・雑音・100 ms ホールドには頑健）、足裏平面は world で約 -30°、COM は支持範囲（x 幅 15 mm）より 73 mm 前、左右ずれは股中点の鏡映で y 残差 1〜3 mm（遠位で x に約 1 cm の非対称）、H_gainDR（damping ×0.5〜2.0）07:55 起動。commit `3382cf8` `d8b2db3`。

```
# 指示: 停止点2・3 の講評と、最後の評価（P6、停止点4 で止まる）

停止点2・3 ありがとう。条件9 が数値不安定ではないと確定し、H_nolinvel の弱点（前進速度・横移動・stiffness 低下）もはっきりした。
判断: H_nolinvel はデプロイ候補にしない。候補は G_real_peak@2999・H_eff13p5@2999・H_gainDR@2999 の3本から停止点4 で選ぶ。
前の指示はすべて有効。GPU の占有は 13:00 まで。下の優先順で進め、13:00 までに終わらない項目は始めずに報告に「未実施」と書く。

## 優先1 H_gainDR の評価（P5-5 どおり）
iter 1000 / 2000 / 2999 で measure_crab.py（S1〜S9、トルク指標込み）、2999 で P1 の条件 1・3・6・7・8・9・10・16。時間が足りなければ 2999 だけでよい。

## 優先2 H_eff13p5@2999 の頑健性（64 env）
P1 の条件 1・3・4・6・8・9・10・16 と、P5-3 の zero（base_lin_vel を 0）。

## 優先3 P6-2 base_lin_vel が途絶えたとき何秒もつか（G_real_peak@2999。H_gainDR が停止点4 で合格なら H_gainDR@2999 でも）
S1 を 3 s 走らせたあと、方策に入る base_lin_vel を (a) 0 にする (b) 最後の値で保持する、を 0.1 / 0.2 / 0.5 / 1.0 / 2.0 s 続けてから真値に戻す。条件ごとに、途絶開始から 3 s 以内の転倒率。T265 が外れたとき何秒以内に止めればよいかを決めるため。

## 優先4 P6-1 足裏 -30° と立ち方（G_real_peak@2999）
1. 既定姿勢で、FFE の関節角を何 rad 足せば足裏平面の pitch が 0 になるか（左右）。その角が URDF リミットの内側か。
2. S3（指令 0）の 2〜10 s と、S1 の立脚中（その足の接触力 > 体重の 20% の step）で、足裏平面の pitch の平均と範囲、FFE 関節角の平均（左右）。方策が足首を曲げて足裏を平らにしているか。
3. 既定姿勢の FFE だけに 1 の補正を入れた姿勢で、action 0（目標角＝その姿勢）・ゲイン既定のとき 2 s 立てるか（32 env、転倒率）。cfg は変えず、評価スクリプト内で初期関節角と目標角だけ上書き。

## 【停止点4】優先1〜4 が終わるか 13:00 になったら報告して止まる（新しい学習は起動しない）
- 冒頭3行: H_gainDR の判定（カニ歩き・立ち往生・転倒・トルク・小旋回）、damping ×0.5 / ×2 で合格したか、3本のうちデプロイに推す1本と理由
- 表: G_real_peak@2999 / H_eff13p5@2999 / H_gainDR@2999 を P1 の条件 1・3・4・6・7・8・9・10・16 と P5-3 zero で横に並べる（S1 転倒率・静止率・生存個体の v_x、S3 転倒率、小旋回判定、FFE の RMS と飽和率）
- P6-2 の表、P6-1 の結果、未実施の項目
- H_gainDR と H_eff13p5 の run フォルダ名、H_gainDR@2999 の ONNX 書き出し（exported_2999 に分けて）と verify_onnx.py の結果、SHA256
- commit のハッシュ（push しない）
推測で直さない。想定外は止まって報告。
```

---

## 追加（2026-09-17 11 時前）H_gainDR の noise_std_type 取り違えへの回答

> WRS 報告: H_gainDR だけ起動行に agent.policy.noise_std_type=log が抜け、rsl_rl 既定の scalar で 3000 iter 学習。優先1 の評価 11 本はチェックポイント読み込み（log_std_param / std_param の不一致）で全滅。優先2（H_eff13p5 の頑健性）は正常に進行中。

```
# 回答: H_gainDR は選択肢1（評価だけ scalar で続行）。優先2 は止めずに完了させてよい

報告ありがとう。原因の特定（rl_cfg.py:81 の既定 scalar、起動行の漏れ）まで出してくれたので判断しやすい。

## 判断
- 選択肢1で進める。noise_std_type は学習中の探索ノイズの持ち方（std をそのまま持つか log で持つか）だけで、評価・ONNX で使う方策の平均出力（MLP）には入らない。デプロイ候補を選ぶのに必要なのは「このチェックポイントが damping ×0.5 / ×2 などで倒れないか」で、それはチェックポイント自体の性質なので、学習条件のずれがあっても評価結果はそのまま使える。
- 留保は「ゲインのランダム化の効果」と「std の持ち方の違い」を切り分けられないこと。これは原因の説明にだけ効き、候補選びには効かない。報告と training_runs 用のメモに「noise_std_type=scalar（他は log）」を必ず書く。
- 選択肢2（log で再学習）は今日はやらない。停止点4 で H_gainDR をデプロイに推すことになったら、別の日に H_gainDR_log として回す（再現性の確認を兼ねる）。

## やること（優先1 を差し替え）
1. measure_crab.py と _play.ps1 / _eval.ps1 が、run フォルダの params\\agent.yaml から noise_std_type を読んで合わせるように直す（effort の上書きを params\\env.yaml から読み直した 5f3a487 と同じ考え方）。.bak を取って、diff を報告に載せる。起動行の手書きに頼らない。
2. 読み込めることを H_gainDR@2999 の S1 だけで先に1本確認（64 env）。念のため G_real_peak@2999 の S1 も1本回し、以前の値（v_x +0.462）と一致すること＝既存 run が壊れていないことを確認。
3. H_gainDR の評価は 2999 を最優先: S1〜S9、P1 の条件 1・3・6・7・8・9・10・16。時間が残れば 1000 / 2000。
4. 学習ログ（TensorBoard）の Policy/mean_noise_std（または同等の項目）の最終値を、H_gainDR と G_real_peak で並べる（探索ノイズの大きさがどれだけ違ったかの参考）。
5. H_gainDR@2999 の ONNX 書き出し（exported_2999 に分けて）と verify_onnx.py。
6. _launch.ps1 に、起動前に最終的な Hydra 上書きの中に noise_std_type=log が入っていなければ警告を出して止まるチェックを入れる（.bak を取ってから。既定の挙動は変えず、明示的に scalar を指定したときは通す）。commit（push しない）。

優先順は: 1〜2 → 優先2 の残り → 3 → 5 → 優先3（P6-2）→ 4・6 → 優先4（P6-1）。13:00 で止めて停止点4 を報告（未実施の項目を明記）。
推測で直さない。想定外は止まって報告。
```
