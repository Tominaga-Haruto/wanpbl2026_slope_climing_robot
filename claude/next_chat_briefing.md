# 次チャットへの使い捨て引継ぎ書 — 2026-09-18 P1実行中

このファイルは次チャット開始時だけ使う。恒久情報を盛り合わせる場所ではない。
次の引継ぎ時に、その時点の内容へ全面的に置き換える。

## 最初に確認すること

1. `git status --short`。
2. ユーザーに、現在見えているP1の最新iter、runフォルダ名、直近ログ10行を貼ってもらう。
3. WRSで `train.py` が動いている間は、絶対に別の学習を起動しない。
4. 学習の新規起動は必ず `tools\runs\_train_foreground.ps1`。`_launch.ps1`、
   `Start-Process`、出力リダイレクトだけのバックグラウンド学習は禁止。

## 現在地

- 目標はまず平地で実機デプロイ。坂と安定したその場旋回は後回し。
- 直進の第一候補は `H_eff13p5@2999`、予備は `G_real_peak@2999`。
- 旋回:
  - `N_w1_seed2` は4498まで完走したが合格チェックポイント0。seed 1の結果は再現しなかった。
  - `N_w1p5` は `model_4400.pt` だけ合格。連続300 iterの採用条件は不合格。
  - 旋回2巡目は回さない。発表では「未達、原因特定済み」とする。
- `P_gainDR_narrow` は、歩けるH_eff13p5@2999を再開し、stiffnessを0.85〜1.15倍、
  dampingを0.8〜1.25倍にランダム化する直進頑健化。夜間は3069で停止し、保存済みは
  model_3000だけだった。
- ユーザー報告では、この引継ぎ作成時点でP1を再度動かしており、うまく進んでいるように見える。
  ただしこのPCからWRSの実プロセスは見えないので、run名とiterは次チャットで実測確認する。
- パッケージ `D:\Tominaga\deploy_pkg\deploy_pkg_20260918.zip` と、H / N_w1p5 / Lの
  短時間動画は作成・起動確認済み。

## 判断規則

- 現在のP1が動いているなら、その1本だけを4498まで完走させる。二重起動しない。
- 完走後、3600 / 4000 / 4498を既存プロトコルで評価する。特に4498で基準条件と
  stiffness 0.7倍、damping 2倍、速度観測途絶を確認する。
- P1@4498が既存基準に合格し、stiffness 0.7倍の転倒率5%以下、かつH_eff13p5に無い
  不合格なしなら、新しい直進候補にする。その場合だけ夜の再現性確認として同設定seed 2を
  H_eff13p5@2999から+1500 iter回す。
- P1が静止率50%超なら、次はstiffnessだけ0.9〜1.1倍、damping固定を候補にする。
- P1が歩くが新しい不合格を出した場合は、夜に推測で追加学習しない。H_eff13p5を維持する。

## 運用上の絶対条件

- 学習は画面にiteration、報酬、noise stdが流れるフォアグラウンドだけ。
- 学習中のPowerShellを閉じない。止めるときはCtrl+C。
- 共用GPUなので開始前に他人のプロセスを確認。他人のプロセスは触らない。
- 実機モーターはこの学習チャットでは動かさない。
- 想定外のエラーや設定差があれば推測で直さず、ログを保存してユーザーへ報告する。

## 次チャットで必要な成果物

- P1のrun名、最終iter、終了コード。
- 3600 / 4000 / 4498の評価表。
- 4498の頑健性比較と、H_eff13p5から候補を替えるかの結論。
- 夜9時間枠にseed 2を回すか、追加学習を止めるかの明示判断。
