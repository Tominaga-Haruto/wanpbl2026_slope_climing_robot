# WRS機 Claude Code の新チャットに貼る文: 夜間の自走（2026-09-18 01:00〜09:45）

> 作成 2026-09-18 00:50、同 01:10 に停止点9a（`tools\\logs\\REPORT_exp09_stop9a.md`、commit `6f70dfd`）を受けて修正。N0 は全部完了、N 本番2本は 00:48 に iter 3049 で走行中。
> 使い方: 下の「ユーザーが先にやること」をやってから、WRS機で Claude Code の新しいチャットを開き、コードブロックを1つ貼る。
> 経緯は `chats/2026-09-18_overnight-and-controller.md`。

## ユーザーが先にやること（WRS機、寝る前に5分）
1. 古い WRS チャット（Sonnet）で Esc を押して止める。ウィンドウは閉じない（起動済みの学習が道連れで止まるのを避ける）。
2. 新しいチャットを開き、`/model` で Opus を選ぶ。
3. 許可の確認で朝まで止まらないようにする（許可を聞かれるたびに止まるので、寝ている間は進まない）。どこまで自動で許すかはユーザーが決める。
4. Windows のスリープを切っておく（電源につなぎ、TeamViewer はつないだままでよい）。
5. 下のコードブロックを貼る。

```
# 新チャットの開始: 夜間の自走（01:00〜09:45）。デプロイ用パッケージ → 学習3本の並走 → 30分ごとの監視と、決めた範囲での自動の手直し → 2巡目 → 朝の報告と再生コマンド

ユーザーはこれから寝る（09:45 まで返事はない）。この文だけで朝まで進めること。途中で質問して止まらない。ここに書いた判断規則で決め、決めた理由を夜間ログに残す。

## 0. 最初に読む（この順。読んだら要点を 5 行で書く）
1. D:\\Tominaga\\slope-climbing-robot\\README.md
2. D:\\Tominaga\\slope-climbing-robot\\legacy/project_handbook.md
3. docs\\experiments\\ の exp06・exp08 の記録と、tools\\logs\\REPORT_exp08_stop8b.md の冒頭（全文は要るときだけ）
4. tools\\logs\\ の実験09 の途中の報告・ログ（REPORT_exp09_stop9a.md があればそれ）
前のチャット（Sonnet）は実験09 の停止点9a まで終えた（REPORT_exp09_stop9a.md、commit 6f70dfd）。
- N0-1〜N0-4 は全部完了。L_angstd_w1 の合格は 4000・4200 だけ（幅 200 iter で規則の 300 に届かず）。S1 の関節トルク RMS は全関節で定格以下。頑健性で H_eff13p5 に無い不合格が2件（stiffness ×0.7 で全シナリオ 12.5〜42.2% 転倒、速度観測の 0 埋め 0.2 s で転倒 9.4%（H_eff13p5 は 1.6%））。
- N 本番2本（N_w1_seed2 / N_w1p5）は起動済みで、00:48 に iter 3049（終わる見込み 02:30 ごろ）。起動し直さない。

## 1. 掟（今までと同じ）
- 共用PC。D:\\Tominaga\\ の外に書かない。システム設定を変えない。nvidia-smi で他人の計算プロセスが出たら、新しく何も起動しない（動いている自分のプロセスはそのまま）。他人のプロセスは止めない。夜間ログに書く。
- 起動は tools\\runs\\_launch.ps1 / _eval.ps1 / _play.ps1（conda activate 込み）。noise_std_type=log を必ず入れる。平地は sub_terrains の proportion 上書き。評価は 1プロセス1env。
- 再開は --resume --load_run --checkpoint をトップレベル引数で。起動行は実際の run の params から写し、64 env / 20 iter のテスト run の params\\env.yaml・agent.yaml を元の run と丸ごと diff してから本番。
- 学習は Start-Process などで Claude Code から切り離して起動し、ログをファイルに出す（チャットが止まっても学習が止まらないように）。
- Edit でハードリンクが切れる。編集後は5組のリンク数と SHA256。
- 破壊的操作は .bak を取ってから。push しない。削除しない。pip / conda で入れない。Isaac Lab 本体を変えない。
- ログは全文を出さない（末尾 30 行や Select-String）。待機は 1 回 5〜10 分の Start-Sleep。

## 2. 最初の 20 分: 状態の確認
- 自分と他人の GPU プロセス（nvidia-smi --query-compute-apps=pid,used_memory --format=csv、Get-Process python の開始時刻）。N_w1_seed2 / N_w1p5 の PID・run フォルダ・ログファイルのパスを特定し、最新 iter を夜間ログに書く。二重に起動しない。
- N の2本が前のチャットのシェルにぶら下がっていて、前のチャットを閉じると止まる作りなら、止めずにそのまま走らせ、夜間ログと朝の報告に書く（前のウィンドウはユーザーが閉じずに残している）。落ちたら §6 の手直し1 で再開する。
- git log -5、git status --short、ハードリンク5組。
- D: と C: の空き（どちらかが 20 GB を切っていたら、学習は2本までにする）。
- 使う run フォルダの実名を確かめる: H_eff13p5（2026-09-17_00-08-51_H_eff13p5）、G_real_peak（2026-09-16_17-28-46_G_real_peak）、H_gainDR（2026-09-17_07-55-38_H_gainDR）、L_angstd_w1、N のスモークテスト。
- tools\\logs\\NIGHT_LOG_20260918.md を作り、以後ここに時刻つきで1行ずつ書く。

## 3. 優先1: 明日の仮デプロイ用パッケージ（02:00 までに。GPU は使っても 16 env）
置き場所: D:\\Tominaga\\deploy_pkg\\20260918\\（リポジトリの外。git に入れない）。方策ごとにフォルダを分ける。
- H_eff13p5_2999\\（第一候補）: policy.onnx、policy.pt、verify_onnx の結果、golden npz、obs_contract.md、各ファイルの SHA256
- G_real_peak_2999\\（予備）: 同じ一式（golden npz が無ければ H と同じ手順で作る）
- L_angstd_w1_4000\\（実験用。その場旋回を吊った状態で見るだけ。頑健性で stiffness ×0.7 と速度観測の 0 埋め 0.2 s が不合格。S1 の関節トルク RMS は定格以下）: 同じ一式
- 共通の DEPLOY_README.md（1〜2 ページ、数値は実物の params と obs_contract から写す。推測で書かない）:
  - 観測 42 次元の並びと scale、行動 10 次元の関節順・scale 0.5・offset（既定関節角）、clip なし、50 Hz（sim.dt・decimation）
  - 指令 (vx, vy, wz) の意味（base 座標）と学習時の範囲。評価で確かめた値（前進 0.5、小旋回 ±0.3）
  - デプロイ用アクチュエータ表（関節ごとの stiffness・damping・effort_limit、グループ）
  - 速度観測が途切れたとき: 直前値保持、0.2 s で停止動作（実験04 と D3 の数値つき）
  - 方策ごとの既知の弱点（damping ×2 で転倒、stiffness ×0.7、base_lin_vel 0 で転倒、L_angstd_w1 は速度途絶 0 埋め 0.2 s で 9.4% など、REPORT_exp04・exp09_stop9a から）
- 最後にフォルダごと zip にする（D:\\Tominaga\\deploy_pkg\\deploy_pkg_20260918.zip）。ユーザーがノートPCへ持っていく。

## 4. 優先2: 学習3本の並走（GPU を朝まで使い切る）
4096 env × 3本。評価を同時に回すのは VRAM の空きが 3.5 GB 以上あるときだけ（4 プロセス目）。GPU 90% 超で env の構築がハングした前例があるので、評価の env 構築が 15 分進まなければそのプロセスだけ止めて、env 数を半分にして1回だけやり直す。

1巡目（各 +1500 iter。3本で 1本あたり 5〜6 s/iter の見込み）:
- T1 N_w1_seed2: 起動済み（L_angstd_w1 と同一、seed 2）。そのまま続ける
- T2 N_w1p5: 起動済み（std 0.35、weight 1.5、seed 1）。そのまま続ける
- P1 P_gainDR_narrow（まっすぐ歩く方の改良）: H_eff13p5 の起動行そのもの（旋回の変更は入れない）に、randomize_actuator_gains だけを足す。上書きの書き方は H_gainDR の run の params\\env.yaml から写し、範囲は stiffness ×0.85〜1.15・damping ×0.8〜1.25 にする。noise_std_type=log（H_gainDR は scalar が漏れていた）。H_eff13p5 の model_2999.pt から再開。§2 の確認が済んだらすぐ（パッケージ作りと並行で）テスト run → 本番を起動する。
  狙い: H_eff13p5 の頑健性の弱点（stiffness ×0.7 で転倒 6.2%、damping ×2 で転倒）を、実機で測ったゲインの誤差の範囲で埋める。H_gainDR（×0.7〜1.3 / ×0.5〜2.0、一から学習）はほぼ全指令で静止したので、狭い範囲・歩ける方策から始める。
- 3本とも、テスト run の env.yaml・agent.yaml の diff を夜間ログに要約（差が意図した項目と run ごとの値だけか）。

1巡目の評価（各ランの 3000 より後の保存チェックポイント全部）:
- T1・T2: S6・S9 を 256 env・評価 seed 2 回（実験09 の N0-1 と同じ）。合格範囲に入ったものは S1〜S11 とトルク。
- P1: model_3600 / model_4000 / model_4498 で S1〜S11 とトルク（64 env）。4498 で頑健性スイープ（実験09 N0-3 と同じ条件セット: c01・c03・c04・c06・c07・c08・c09・c10・c16・速度途絶 0 埋め 0.2 s / 保持 0.5 s）。H_eff13p5@2999 の既存の結果と並べる。
- 保存間隔は 200 iter（3000, 3200, …, 4498）。

## 5. 判断規則（結果を見てから変えない）
その場旋回の合格ライン（実験06 と同じ）: S6 / S9 で |yaw| ≥ 0.25・符号一致・左右比 0.5〜2、|(vx, vy)| ≤ 0.10、転倒 ≤ 5%、左右とも足を上げる、既存基準（前進速度・カニ歩き・転倒・トルク・小旋回 S7/S8）を落とさない。
「合格範囲」: 同じ run の保存チェックポイントで、連続 300 iter 以上が合格（評価 2 回とも）。

旋回の2巡目（1本）:
- (a) T1 か T2 に合格範囲がある → その範囲の真ん中のチェックポイントで頑健性スイープ。不合格が c06（stiffness ×0.7）だけなら、2巡目は「そのチェックポイントから +1000、P1 と同じ狭い randomize_actuator_gains を足す」（O_turn_gainDR）。ただし P1 が静止に落ちていたら（S1 シナリオで静止率 > 50%）これはやらず (c) へ。
- (b) 合格範囲はないが、T2（weight 1.5）の合格チェックポイント数が L_angstd_w1（2 個: 4000・4200）より多い → 2巡目は weight 2.0・std 0.35・seed 1 で H_eff13p5@2999 から +1500（O_w2）。
- (c) それ以外 → 2巡目は L_angstd_w1 の起動行で、その場旋回の指令の割合 rel_turn_in_place_envs だけ 0.2 → 0.35 にして H_eff13p5@2999 から +1500（O_turn35）。

まっすぐの2巡目（1本）:
- P1 が既存基準に合格し、c06 の転倒 ≤ 5% で、H_eff13p5 に無い不合格を出さない → P1 を「まっすぐの新候補」とし、2巡目は同じ起動行の seed 2（P2_seed2、再現性）。P1@4498 のパッケージを §3 と同じ形で deploy_pkg\\20260918\\P_gainDR_narrow_4498\\ に作る。
- P1 が静止に落ちた（S1 シナリオで静止率 > 50%） → 2巡目は stiffness だけ ×0.9〜1.1、damping は変えない（P2_stiffonly）。
- それ以外（歩くが新しい不合格がある） → まっすぐの2巡目はやらず、そのぶん旋回の2巡目の候補を2本にする（(a)〜(c) で選ばれなかった中で、番号の若いもの）。

2巡目の起動の締め切り:
- 05:30 までに起動できれば +1500。05:30〜07:00 は、08:30 に終わる iter 数に減らす（そのときの秒/iter から計算して夜間ログに書く）。07:00 を過ぎたら新しい学習は起動しない。
- 2巡目の評価は 1巡目と同じ。09:15 までに終わらない評価は、最後の保存チェックポイントだけにする。

## 6. 30 分ごとの見回りと、自分でやってよい手直し
Claude Code の定期実行の仕組み（/loop や wakeup）が使えるならそれで 30 分ごと、無ければ Start-Sleep を 10 分以内で重ねて 30 分ごとに:
- 各学習: 生きているか、最新 iter と秒/iter、終わる見込み、Mean action noise std（0.1 を切って下がり続けていないか）、ログに nan / Traceback / CUDA error がないか、正規化 error_vel_xy
- nvidia-smi（VRAM、温度 85°C 超、他人のプロセス）、D: と C: の空き
- 夜間ログに1行（時刻・各ランの iter・異常の有無・やったこと）

自分でやってよい手直し（これ以外はやらない。やったら理由と前後の数値を夜間ログに）:
1. 学習プロセスが落ちた: 原因を Traceback の要点で記録し、同じ起動行で最新の保存チェックポイントから1回だけ再開する。2回目に落ちたらその枠は諦め、判断規則で次に回す予定だった1本を前倒しする。
2. VRAM 不足・env 構築のハング: その1本だけ num_envs を 4096 → 2048 にして1回だけやり直す（env 数を変えたことを結果の表に書く）。
3. noise std が 0.05 を切った・nan: その run を止め、200 iter 前のチェックポイントまでを有効とする。再開はしない。
4. 評価のハング: そのプロセスだけ止め、env 数を半分にして1回だけやり直す。
5. 他人の GPU プロセスが出た: 新しい起動をやめ、動いている自分の学習は続ける。
報酬・指令・地形・アクチュエータの設定を、§4・§5 に書いた以外に変えない。コードの修正が要る不具合は直さず、その枠を諦めて夜間ログと朝の報告に書く。

## 7. 朝（09:30 までに）
- 学習プロセスが全部終わっているか止まっていることを確認（09:30 に動いているものは、最後の保存チェックポイントを残して止める）。
- 報告書 tools\\logs\\REPORT_night_20260918.md: 1巡目・2巡目の全ランの表（run 名、起動行の差分、iter、合格範囲、S1 v_x、S6 / S9 yaw、転倒、FFE RMS、頑健性の不合格）、判断規則をどう適用したか、夜間の手直しの一覧、deploy_pkg の中身と SHA256。
- commit（コードと docs と報告書だけ。push しない）。docs\\experiments\\ に実験09 と夜間の記録。
- 再生コマンドの確認: 次の §8 のコマンドを、それぞれ 16 env・短い長さ（--video_length 50 など）で実際に1回ずつ起動し、落ちずに始まることを確かめてから載せる。確認に使った動画は消さずに残す。

## 8. 09:45 にチャットに出すもの（Start-Sleep で 09:45 まで待ってから出す）
tools\\logs\\MORNING_0945.md に同じ内容を書き、チャットにも出す。
- 冒頭3行: 明日（今日）の仮デプロイに持っていく方策の結論（まっすぐ / 旋回の実験用）、夜の学習でその場旋回が安定したか、まっすぐの改良が合格したか
- 判定の表（全ラン × 合格範囲・S1・S6/S9・c06・c09）
- 再生コマンド（すべて PowerShell の1行、実行ディレクトリを明示、山括弧なし、run 名とチェックポイント番号を実名で）:
  1. 普通に見る用（ウィンドウあり、16 env、平地。TeamViewer 越しで GUI が落ちたら動画用で見ると書き添える）: H_eff13p5@2999、旋回の一番良いチェックポイント、P_gainDR の一番良いチェックポイント（合格していなくても見る用に）の3本
  2. 動画用（--headless --video --video_length 500、16 env、平地）: 同じ3本と、動画が出るフォルダのパス
  3. 止め方（Ctrl+C。止めてもチェックポイントは消えない）
- 未実施・諦めた枠・異常

## やらないこと
§4〜§6 に書いた以外の設定変更、push、削除、インストール、Isaac Lab 本体の変更、他人のプロセスに触ること。朝まで質問で止まらない。規則で決められないことは、安全な側（起動しない・止める）を選んで夜間ログに書く。
```
