# 指示書 実験09: L_angstd_w1 の再現性・安定性と、旋回できる方策の頑健性（WRS機、実験08 と同じチャットに貼る）

> 作成 2026-09-17 深夜。停止点8b（`tools\\logs\\REPORT_exp08_stop8b.md`、commit `f7131f4`）を受けて。経緯は `chats/2026-09-17_exp08-stop8b-review.md`。

```
# 指示: 停止点8b の講評と、実験09（L_angstd_w1 の再現性と安定性、旋回候補の頑健性。停止点9a・9b）

停止点8b ありがとう。archive/wrs_new_chat_start.md の掟・作法はすべて有効。評価は 1プロセス1env。

## 講評
- 効いたのは weight 1.0。std 0.35 だけ（weight 0.5）は全チェックポイント不合格。
- L_angstd_w1@4000 の合格は、S6 が下がり（+0.319 → +0.268 → +0.122）、S9 が強くなる（-0.112 → -0.353 → -0.422）途中の交差点で取れた形。回る力は付いたが、左右の釣り合いが収束していない。1点だけの合格ではデプロイ候補にしない。64 env・1回の評価のばらつきも分かっていない。
- 確認: 「+500」が model_3600 になっている理由（保存間隔か、表記ミスか）を1行で。
- FFE RMS はデプロイ基準（S1 で RMS ≤ 定格 AK80 9 N·m）の必須項目なので、今回実装する。
- 停止点8a の L0-1「歩くと 1 step あたり増える罰」の1行を 9a で再掲。

## 事前に決めた判定規則（結果を見てから変えない）
- 旋回候補として採用する条件: 同じ run の保存チェックポイントで、連続して 300 iter 以上の範囲がその場旋回の合格ライン（実験06 と同じ）を満たす（256 env・評価 seed 2 回とも）。かつ既存基準の退行なし、かつ頑健性スイープで H_eff13p5@2999 に無い不合格を出さない。
- 満たすものが無ければ旋回候補なし。直進のデプロイ候補は H_eff13p5@2999（第一）・G_real_peak@2999（予備）のまま。

## N0（評価と道具。学習と並走してよい。GPU は既知の上限 3 プロセスまで）
N0-1 密な評価: L_angstd_w1 の保存済みチェックポイントを 3000 より後すべて、S6・S9 を 256 env で。評価 seed を変えて各2回。
  表: iter / S6 yaw / S9 yaw / 左右比 / |(vx, vy)| / 転倒率 / 歩数 / 判定（2回それぞれ）。合格が連続する範囲があるか。
N0-2 関節ごとの computed torque の RMS・最大・飽和率を measure_crab.py に追加（既存の computed torque・飽和率の取り方を grep して流用し、新しく作らない）。
  H_eff13p5@2999 の S1 で出し、実験03・04 の飽和率と矛盾しないことを確認してから使う。矛盾したら止まって報告。
N0-3 L_angstd_w1@4000 の頑健性スイープ: 実験04 停止点4 と同じ条件セット（番号・中身は前回の REPORT をそのまま使う）＋速度観測の途絶（0 埋め 0.2 s・直前値保持 0.5 s）。
  S1・S6・S7・S8・S9。H_eff13p5@2999 の既存の結果と並べ、「H_eff13p5 に無い不合格」を列挙。
N0-4 docs\\experiments\\ に実験08 を正式記録。commit（push しない）。

## N 本番（2本同時、4096 env）
土台は L_angstd_w1 の起動行そのもの（H_eff13p5 の model_2999.pt から --resume --load_run 2026-09-17_00-08-51_H_eff13p5 --checkpoint model_2999.pt、+1500 iter）。変えるのは1本につき1つだけ。
1. N_w1_seed2: L_angstd_w1 と同一、seed を 2 に
2. N_w1p5: std 0.35、env.rewards.track_ang_vel_z_exp.weight=1.5（seed 1）
nsave_interval は L と同じ（変えない）。
起動前の確認（64 env / 20 iter）:
- params\\env.yaml と params\\agent.yaml を L_angstd_w1 の run と丸ごと diff。差が seed（1本目）・weight（2本目）・run ごとの値だけであること。
- ログに Loading model checkpoint が出て、iter が 3000 から始まること。
- 想定外があれば起動せず報告。

## 評価
- 両ランの保存済みチェックポイントを 3000 より後すべて、S6・S9 を 256 env・評価 seed 2 回（N0-1 と同じ手順）。
- 合格範囲に入ったチェックポイントだけ、S1〜S11 全項目と N0-2 のトルク。

## 【停止点9a】N0-1〜N0-4 と2本の起動（iter 30）が終わったら報告して止まる（学習は回したまま）
- 冒頭3行: L_angstd_w1 に合格が連続する範囲があるか / S1 の関節トルク RMS が定格以下か（FFE・HFE は 9、他は 18） / 頑健性で H_eff13p5 に無い不合格があるか
- 表: N0-1 の表、N0-3 の「H_eff13p5 に無い不合格」の表
- 起動行2本、diff の結果、終わる見込み、model_3600 の件、歩くと増える罰の1行
- 詳細は tools\\logs\\REPORT_exp09_stop9a.md に

## 【停止点9b】両ランの +1499 までの評価が終わったら報告して止まる
- 冒頭3行: seed 2 でも合格範囲が出るか / weight 1.5 で左右の釣り合いが安定するか / 既存基準の退行の有無
- 表: ラン別の合格範囲（iter の範囲、なければ「なし」）と、その範囲の S6・S9 の平均
- 判定規則を満たすものがあれば、合格範囲の中央のチェックポイントを1つ推し、ONNX 書き出し・verify_onnx.py・SHA256。頑健性スイープ（N0-3 と同じ）もそのチェックポイントで
- チャットには冒頭3行・表・未実施だけ。詳細は tools\\logs\\REPORT_exp09_stop9b.md に

## やらないこと
上に書いた以外の報酬・指令・地形・アクチュエータの変更、push、削除、インストール。推測で直さない。想定外は止まって報告。
```
