# WRS 実験19: 発表用シム動画の最終追加（学習途中の 4096 体・横指令の再現・旋回の予備）

作成 2026-09-25。実験17（`../inactive/wrs_experiment17_presentation_videos_retake2_instruction.md`）への目視所見を受けた最終追加。
採用済み（撮り直さない）: 02 立ち往生、04 改善後、05 平地、06 坂 5°/3°、10 歩きながら旋回。08 でこぼこは断念。
03 は、URDF 修正前のカニ歩き（機体の横が「前」になっていて、前進指令で横に歩いた）の再現映像として、H_eff13p5@2999 に横向きの指令を与えて撮る。
終わったらこのファイルを `../inactive/` へ移す。

```
# 依頼: 発表用シム動画の最終追加（3 場面）。今日の発表に使う。合計 60 分以内

## このプロンプトの前提
- このプロンプトだけで完結している。AGENTS.md、claude\ 以下の文書、過去の報告書、メモリは読みに行かない
  （自動で読み込まれたものは無視してよい）。
- 撮影は tools\record_fixed_cmd.py（実験17の版）をそのまま使う。スクリプトは変更しない。
  起動の形・Start-Process での出力取得は実験17と同じ。follow カメラは必ず --cam_smooth 0.1 --cam_fix_z を付ける。
- 何も削除しない。pip で何も入れない。学習は起動しない。git の commit・push はしない。
- 出力先: D:\Tominaga\slope-climbing-robot\tools\videos_presentation_20260925_v4\
  1920×1080。ファイル名 <番号><枝番>_<場面>_<条件>_<run短縮名>_<ckpt>.mp4。

## 優先順（時間が足りなければ下から削る）

### 1. 01 学習途中の 4096 体（最優先）
- run 2026-09-17_00-08-51_H_eff13p5。保存点は model_0（無ければ実在する最小）・50・100・200・300・400・600・800・1200・2000・2999
  のうち実在するもの（無い番号は近い実在点）。
- 条件: flat、4096 体、--tiles_rows 64 --tiles_cols 64 --tile_size 2.5、(0.3,0,0)、20 s（episode_s 40）。
- カメラは実験17で正常だった 01a（close）と 01b（medium）の2種類だけ。遠景（01c/01d/01e の画角）は
  フレームの半分が黒くなるので使わない。01a と 01b の実際の --cam_eye / --cam_lookat を実験17の起動行から写す。
- 各保存点の転倒率・前進速度も表にする（学習が進むにつれ歩けるようになる様子を数字でも示すため）。
- 撮影順: 01b の画角で全保存点 → 時間が残れば 01a の画角で 0・200・800・2999。

### 2. 03 横向き指令（カニ歩きの再現映像）
- H_eff13p5@2999、flat、3 体（実験17と同じ 1 体 1 タイル）、30 s。
- 指令: (0, 0.2, 0)・(0, 0.3, 0)・(0.15, 0.15, 0) の3本。
- それぞれ --cmd_vis なし（発表用）と、あり（確認用）の2本。カメラは follow（実験17の 03 と同じ）と真上寄り eye (0,-4,5)。
  多ければ --cmd_vis なし × 2 カメラを優先。

### 3. 09 その場旋回の予備（時間が残ったときだけ。1 本 30 s）
- 実験17の N_w1p5@4400 は、見た目では足をひねっているだけで回っていなかった（指標では合格していた）。
- 候補: L_angstd_w1（run 2026-09-17_21-18-22_L_angstd_w1）の model_3600 と model_4200。指令 (0,0,+0.3) と (0,0,+0.5)。
- カメラは真上寄り (0,-0.5,4) と斜め上 (1.5,-1.5,3.0)。
- 評価の追加: 30 s 間の胴体の累積回転角 [deg]、左右の HR（股のひねり）関節の角度の 5〜95 % 幅。
  累積回転が 90°未満の組は撮らずに表だけ出す。

## 報告（これで止まる）
1. 冒頭3行: 撮れたもの、01 で歩き出した保存点（前進速度が 0.2 m/s を超えた最初の点）、09 の結果。
2. 01 の表（保存点 × 転倒率・前進速度 × ファイル名）。
3. 03・09 の表（条件 × カメラ × 数値 × ファイル名）。
4. 撮り直し用の起動行（場面ごとに1行、実名入り、山括弧なし）。
```
