# 共通ドキュメント領域

この`claude/`はClaude専用ではない。Codex、Claude Code、ユーザーが共有するプロジェクト記録である。

リポジトリ直下の`AGENTS.md`を先に読み、その後に次を順に読む。

1. `START_HERE.md`
2. `CONTEXT.md`
3. `handoffs/ACTIVE.md`

`reports/`は根拠を残す報告書、`handoffs/`は次の担当のための短い現在地、`chats/`は過去の会話履歴である。生CSV・重み・鍵は置かない。
