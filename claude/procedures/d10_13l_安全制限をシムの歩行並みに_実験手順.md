# D10-13L: 自動停止の上限をシムの歩行並みに広げて H_eff13p5@2999 を床で（要承認）

作成 2026-09-25。build `D10_13L_WIDE_20260925`（Connect2USB2CAN `1b610cf`、`.bak_20260925_wide`、テスト 166＋30 本 OK）。根拠 `../reports/2026-09-25_実機で歩けない原因の再検討_安全制限とシムの歩行.md` §1・§5-B。

## 何が変わるか（`--wide-limits`、`--walk-limits` と `--tilt-abort-deg` が必須）

| 項目 | D10-13K（`--walk-limits`） | D10-13L（`--wide-limits`） |
|---|---|---|
| 電流中止 下から1番目 FFE | 10 A | **24 A** |
| 下から2番目 KFE | 10 A | **16 A** |
| 下から3番目 HFE | 11 A | **24 A** |
| 下から4番目 HAA | 6 A | **10 A** |
| 下から5番目 HR | 3 A | **6 A** |
| 速度中止 | 200（最大 400）°/s | **900°/s**（`--walk-speed-abort-dps` で 200〜900） |
| slew の上限 | 200°/s | **1000°/s**（起動行では 800） |
| 傾き 30°・stale・モーターエラー・CAN・角度の中止・中止後保持・safe-clamp | そのまま | そのまま |

AK80-9 の 24 A は方策の effort 13.5 N·m（25.8 A）の手前。

## 前提（毎回）

- 吊り紐を持つ人と、主電源を切る人を必ず置く。暴れ方は D10-13K より強くなる。
- 左脚の CAN 断は大きく動いた直後に起きている。起きたら続けずに電源と配線を見る。
- D7 の原点合わせと床への置き方は D10-13K と同じ（`d10_13k_しゃがみ姿勢から開始_最後の実験手順.md`）。
- CSV 名は run ごとに変える（下のコマンドは L1〜L3 で別名）。

## コマンド（実行フォルダ C:\Users\harut\Connect2USB2CAN）

L1 その場（vx 0）:
```powershell
cd C:\Users\harut\Connect2USB2CAN; .venv310\Scripts\python.exe ver9_d8_sender.py --arm --all-axes --walk-limits --wide-limits --soft-stop-seconds 3 --start-gate-deg 5 --fine-timer --zero-offset cad_fk --knee-trim-deg 19 16 --start-pose crouch --safe-clamp --origin-ref stand --tilt-abort-deg 30 --stand-seconds 15 --policy-slew-dps 800 --package C:\Users\harut\wanpbl2026_slope_climing_robot\H_eff13p5_2999\H_eff13p5_2999 --ramp-seconds 10 --duration 10 --csv logs\d10_13l_l1_wide_vx0.csv --vx 0 --vy 0 --wz 0
```

L2 歩行（vx 0.3）:
```powershell
cd C:\Users\harut\Connect2USB2CAN; .venv310\Scripts\python.exe ver9_d8_sender.py --arm --all-axes --walk-limits --wide-limits --soft-stop-seconds 3 --start-gate-deg 5 --fine-timer --zero-offset cad_fk --knee-trim-deg 19 16 --start-pose crouch --safe-clamp --origin-ref stand --tilt-abort-deg 30 --stand-seconds 15 --policy-slew-dps 800 --package C:\Users\harut\wanpbl2026_slope_climing_robot\H_eff13p5_2999\H_eff13p5_2999 --ramp-seconds 10 --duration 10 --csv logs\d10_13l_l2_wide_vx03.csv --vx 0.3 --vy 0 --wz 0
```

L3 もう一度（vx 0.3。L2 と同じ条件、CSV 名だけ変える）:
```powershell
cd C:\Users\harut\Connect2USB2CAN; .venv310\Scripts\python.exe ver9_d8_sender.py --arm --all-axes --walk-limits --wide-limits --soft-stop-seconds 3 --start-gate-deg 5 --fine-timer --zero-offset cad_fk --knee-trim-deg 19 16 --start-pose crouch --safe-clamp --origin-ref stand --tilt-abort-deg 30 --stand-seconds 15 --policy-slew-dps 800 --package C:\Users\harut\wanpbl2026_slope_climing_robot\H_eff13p5_2999\H_eff13p5_2999 --ramp-seconds 10 --duration 10 --csv logs\d10_13l_l3_wide_vx03.csv --vx 0.3 --vy 0 --wz 0
```

判定:
```powershell
cd C:\Users\harut\Connect2USB2CAN; foreach ($f in 'd10_13l_l1_wide_vx0','d10_13l_l2_wide_vx03','d10_13l_l3_wide_vx03') { .venv310\Scripts\python.exe ver9_d8_sender.py --analyze logs\$f.csv | Select-String 'SAGITTAL|POLICY START|ABORT RECORD|TILT at' }
```

起動直後に `WIDE LIMITS` の行と、電流の表（FFE 24.0A など）が出ることを確かめてから床に降ろす。
