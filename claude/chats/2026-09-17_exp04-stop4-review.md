
# 2026-09-17 実験04 停止点4 の講評、デプロイ方策の決定、実験05（デプロイ準備＋その場旋回の原因調べ）

## やったこと
- README と `next_chat_briefing.md`（昼版）を読み、ユーザーが貼った WRS の停止点4 報告（`tools\\logs\\REPORT_exp04_stop4.md`、13:21 に P6-1 のハングで停止）を講評した。
- デプロイ方策をユーザーに確認 → 「両方持っていく」。GPU は「ずっと使える」。
- ユーザーから「その場旋回ができないのは流石にまずい。デプロイ準備と旋回直しを同時に進めたい」。→ 実験05 の指示書 `wrs_experiment05_instruction.md` を作成（デプロイ準備 D ＋ 旋回の原因調べ T0、停止点5a・5b）。

## 決めたこと・分かったこと
- **停止点4 の結果（@2999）:**
  - H_gainDR: ほぼ全指令で完全静止（S1 静止率 1.0、小旋回も不合格）。damping ×0.5 / ×2 の転倒 0% は動いていないだけ。Policy/mean_std 最終 0.244（G 0.439）。**候補から外す。**
  - 共通条件（1・3・4・6・8・9・10・16・速度 0）の不合格数は **G_real_peak 3（damping ×0.5 の直進 yaw 0.129、damping ×2 転倒 100%、速度 0）／ H_eff13p5 3（stiffness ×0.7 転倒 6.2% = 4/64、damping ×2 転倒 96.9%、速度 0）で同点。**
  - WRS は G_real_peak を推したが、前チャットの選び方「同点ならトルクの低い H_eff13p5」と食い違う → Cowork は H_eff13p5 を推奨。
  - 根拠: ゲインのずれ（stiffness・damping）は実機で換算係数を測れば指令側で合わせられる。測っても消えないのは Kt と発熱 → 条件4（AK80 上限 9）で G 転倒 3.1%・FFE 飽和 40%、H_eff13p5 0%・23%。S1 の FFE RMS 7.04 / 5.93（定格 9）。G の利点は S1 v_x 0.462 / 0.419 だけ。
  - G_real_peak@2999 の S1 v_x 0.462 は以前と一致（評価系は壊れていない）。
- **ユーザー決定: G_real_peak と H_eff13p5 の両方を持っていく（実機は H_eff13p5 から、G は予備）。** 観測は同じ 42 次元なので制御ループは共通（D2 で項ごとに確認させる）。
- **damping ×2 はどの方策も不合格のまま → 実機の Kd 分離（M2）はデプロイ前の必須関門で確定。** ゲインランダム化の再学習は今はしない。
- **速度観測の途絶（P6-2、G_real_peak）:** 0 埋めは 0.2 s まで転倒 0%・0.5 s で 97%。直前値保持は 0.5 s まで 0%・1.0 s で 19%。→ 制御ループは「途絶したら直前値を保持し、0.2 s で停止動作」。H_eff13p5 でも測る（D3）。
- P6-1（足裏）はハングで未実施。GPU 90% 超での env 構築ハングと同じ症状の可能性 → env 16 からやり直し（D4）。
- 報告に commit `1b93c48` の diff が無い → D1。
- **その場旋回（ユーザー判断: 今のままではまずい）:** 事実は S6 / S9 で |yaw| 0.03 前後、S7 / S8 は合格、旋回指令は十分出ていた、error_vel_yaw は iter 1600 以降悪化。
  - Cowork の疑い（未確認・T0 で grep と実測）: ①足上げの報酬が並進の指令の大きさでしか入らない（Isaac Lab の biped 版はそういう実装のことがある）→ その場旋回の指令では足を上げる得がない ②その場旋回の指令（並進ほぼ 0 ＋ |wz| 大）の割合が小さい ③track_ang_vel_z_exp の weight 0.5 が小さい ④joint_deviation_hip が HR（股のひねり）を罰する。
  - 学習の変更は停止点5a の数字を見て決める。暫定の合格目安は「S6 / S9 で |yaw| が指令の半分（0.25 rad/s）以上、S1 など既存の合格を落とさない」（ユーザー未確認）。
  - 直した方策は、デプロイ前に頑健性の評価をやり直す必要がある。

## 手を動かした場所
- 新規: `claude/wrs_experiment05_instruction.md`、この記録
- 更新: `claude/README.md`（現在の状況・アクティブな指示書・索引）

## 積み残し・次にやること
- ユーザーが `wrs_experiment05_instruction.md` のコードブロックを WRS に貼る。
- 停止点5a（T0: その場旋回の原因の数値）→ 旋回を直す学習を設計（H_eff13p5@2999 からの再開と一からの2本並走が第一案。学習中は env 構築ハングの危険があるので、評価は先に済ませる）。
- 停止点5b（D: golden npz と obs_contract の H_eff13p5 版、P6-2 の H_eff13p5 版、P6-1、H_gainDR の学習ログ）。
- `training_runs.md` の H_gainDR 行と停止点4 の節、`next_chat_briefing.md`、`wrs_training_history.md` §7 は未更新（この記録と README が新しい）。
- 未 push のコミットに `1b93c48` が増えた。
- 制御ループ（途絶時は直前値保持・0.2 s で停止）を書くには、ノートPCの `Connect2USB2CAN` フォルダの接続が要る。
