# 2026-09-22 D9-6: 3軸とも動いた。C は判定式のバグだった

## やったこと

- D9-4（無通電確認）→ D9-5A（0x1C +6.5°）→ D9-6（0x1C +6.5°／0x2B −2.7°／0x13 −6.5°）を1日で回した。
- ユーザーは支持具が無いため、**手でだいたいの初期姿勢に合わせて D7 の原点を取り直し、手で支えながら**各1回実行した。
- 保存 CSV を `--analyze` に掛け、位置と電流の時系列を1サンプル単位で追った。
- `ver9_d8_sender.py` を2回修正（`D9_DIRSTALL_20260922_2300` → `D9_LATEBREAK_20260923_0010`、`.bak` 各あり）。テスト 44 → 51 → **54本 OK**。

## 決めたこと・分かったこと

- **0x1C は故障していない。3軸とも判定 A。** 動き出し電流 0.90 / 0.63 / 0.72 A、不感帯 6.50° / 4.55° / 2.17°。`TORQUE PATH` は3本とも verified。
- **これまでの C は判定式のバグだった。** 3本とも hold の最後の1秒で滑り出し、目標へ走りながら電流が落ちていた。正味変位を hold 区間**全体の中央値**で出していたので、動く前の静止部分に乗って 0x1C の実際の +3.50° が +0.200° と読まれていた。→ **hold の最後25%** で見るように変更。
- **動き出し電流の読み方も誤っていた。** 「最初に動いたサンプルの電流」ではその時点でもう誤差＝電流が崩れている。0x13 を 0.02 A と読んでいたが、直前の保持は 0.63 A。→ **直前 0.2 s の最大 |I|** に変更。
- **接触仮説（D9-4）は取り下げ。** 0x13 は爪先内向き −6.5° で 4.1° 動いている。D9-4 報告書の本文・追記は D9-6 報告書で上書きされる。
- **D9-3 / D9-5A は新ビルドでも C のまま。** 本当に量子化ディザで走り出していない。修正が都合よく過去を塗り替えてはいない、と確認した。
- **`DEMONSTRATED_STALL_CURRENT_A` を空にした。** 0x1C は 0.90 A で動いたので、静止記録はもう真ではない。ガード自体は残し、テストは擬似エントリで検証。
- **残る本当の問題は不感帯。** 実機 HR 4.55〜6.50°、FFE 2.17° に対し、シムの friction 0.22 N·m は 0.9〜1.3°。**2〜5倍。** friction スイープの保留を解除し、最優先に上げた。
- **HR の機構的可動域（無通電・手回し）: 外 約90° / 内 約45°（配線長）。** URDF は ±π のまま。方策が内側 45° を超えると配線を引っ張る。
- **この測定の弱点を報告書に明記した。** 手で支えたこと、原点を手で取り直したこと、未測定の向きと軸。

## 手を動かした場所

- `Connect2USB2CAN/ver9_d8_sender.py`、`test_ver9_d8_sender.py`（`.bak_20260922_dirstall`、`.bak_20260922_latebreak`）。
- `wanpbl2026_slope_climing_robot/claude/`: `reports/2026-09-22_d9-6_result.md` 新設、`reports/2026-09-22_d9-4_result.md`（追記済み・取り下げ）、`procedures/d9_6_支持を直してHRとFFE_実験手順.md`、`CONTEXT.md` 更新。
- commit と push はユーザー側。

## 積み残し・次にやること

1. **WRS機で friction 3〜5倍の頑健性スイープ（H_eff13p5@2999）。** 最優先。実機に触らない。
2. 立たせた基準姿勢で D7 の原点を取り直す。
3. HR の可動域 外90°/内45° を URDF・シムへ。
4. 未測定: 0x1C の負方向、0x2B の正方向、他7軸。
5. コントローラー乾式統合、方策Bの資格評価。

---

## 追記（同日・次の実験の設計まで）

- **次の実験を実験11（WRS friction スイープ）に決めた。** 指示書 `wrs_experiment11_friction_instruction.md`、引き継ぎ `handoffs/2026-09-23_exp11_friction_sweep.md`。実機には触らない。評価のみで学習は起動しない。
- **Kt を使わずにシムと実機を突き合わせる式を立てた: `friction 相当 [N·m] = 動き出し電流 [A] × c_p`。** シムの不感帯 = friction/stiffness、実機の不感帯 = 電流/Kp_cmd、Kp_cmd = stiffness/c_p なので stiffness が消える。実測 → HR 0.79〜1.13 N·m（シム 0.37 の 2.1〜3.1×）、FFE 0.38 N·m（シム 0.22 の 1.7×）。**c_p の定義から出る等価であって Kt の独立実測ではない**と指示書に明記した。
- スイープは F1.0 / F2.0 / F3.0 / F5.0 / Fmeas の5条件。**時間が無いとき用に「最短ルート」（F1.0 / F3.0 / F5.0 の3条件）を用意した。**
- 判定 A（F3.0 が F1.0 と同じ合否）なら床上デプロイ準備へ、C（F2.0 で落ちる）なら friction 込みの再学習を設計。
- **`AGENTS.md` に3つのルールを追加した。** (1) commit と push はエージェントが行い、シェルが使えないセッションでは完成形のコマンドを毎回出す。(2) 頼まれていない文書を増やさない。(3) 実験結果は本人の申告ではなく保存ログで判定し、CSV名は実行ごとに変える。
