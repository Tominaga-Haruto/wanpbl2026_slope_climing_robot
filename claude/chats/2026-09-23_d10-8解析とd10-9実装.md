# 2026-09-23 D10-8 の解析と D10-9 の実装（Cowork）

- ユーザー所見: R1・R2 とも初期姿勢はよい。方策に入ると足幅を狭めて当たった（R1 はしっかり当たって終わり、R2 は漕ぐ・歩くような動き）。`--analyze` は未実行だった。
- `--analyze` と `_obs.csv` の集計、onnxruntime の再生（記録と誤差 0）、傾きを消した入れ直し、URDF 順運動学で判定。結果は `reports/2026-09-23_d10-8_result.md`。R1 判定1、R2 判定2、判定3 なし、判定4 あり、中止なし、電源の電流は未報告。
- 主な発見: シムは足首間隔 約 18 cm で歩くが、実機は 24〜29 cm で当たった。
- 途中で順運動学の `<axis>` の向きを読み落として誤った数字を一度出し、直した（報告書に記録）。
- D10-9 を実装: 送信器 `--haa-close-deg`（build `D10_9_HAASWEEP_20260923_1500`、テスト 138本 OK）。手順 `procedures/d10_9_HAA閉じ_足の接触角_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-9_足の接触角.md`。D10-8 の引き継ぎは `inactive/` へ移した。
- 途中で PC との接続が一度切れ、手順書は再接続後に書き込んだ。

## 続き: D10-9 の判定と D10-10 の実装

- ユーザー所見: ゼロ点合わせはほぼ完ぺき。初期姿勢で少し足幅が狭くなり（左右差あり）、閉じていくと左右のつま先が平行でないためつま先だけが触れた。この程度なら無視して次へ進みたい。`--analyze` は未実行だった。
- `--analyze` と時系列で判定: 触れ始め HAA −8.5°、止まったのは −10.3 / −11.0°、R1・R2 で同じ。結果は `reports/2026-09-23_d10-9_result.md`。本人の判断で許容して床上へ。
- D10-10 を実装: 送信器 `--floor-limits`（KFE・FFE 6.0 A、保持 ≤30 s、2 s ごとの電流表示。build `D10_10_FLOORSTAND_20260923_1600`、テスト 143本 OK）。手順 `procedures/d10_10_吊り紐つき床上立ち_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-10_吊り紐つき床上立ち.md`。D10-9 の引き継ぎは `inactive/` へ移した。
