# 2026-09-23 D10-8 の解析と D10-9 の実装（Cowork）

- ユーザー所見: R1・R2 とも初期姿勢はよい。方策に入ると足幅を狭めて当たった（R1 はしっかり当たって終わり、R2 は漕ぐ・歩くような動き）。`--analyze` は未実行だった。
- `--analyze` と `_obs.csv` の集計、onnxruntime の再生（記録と誤差 0）、傾きを消した入れ直し、URDF 順運動学で判定。結果は `reports/2026-09-23_d10-8_result.md`。R1 判定1、R2 判定2、判定3 なし、判定4 あり、中止なし、電源の電流は未報告。
- 主な発見: シムは足首間隔 約 18 cm で歩くが、実機は 24〜29 cm で当たった。
- 途中で順運動学の `<axis>` の向きを読み落として誤った数字を一度出し、直した（報告書に記録）。
- D10-9 を実装: 送信器 `--haa-close-deg`（build `D10_9_HAASWEEP_20260923_1500`、テスト 138本 OK）。手順 `procedures/d10_9_HAA閉じ_足の接触角_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-9_足の接触角.md`。D10-8 の引き継ぎは `inactive/` へ移した。
- 途中で PC との接続が一度切れ、手順書は再接続後に書き込んだ。

## 続き: D10-9 の判定と D10-10 の実装

- ユーザー所見: ゼロ点合わせはほぼ完ぺき。初期姿勢で少し足幅が狭くなり（左右差あり）、閉じていくと左右のつま先が平行でないためつま先だけが触れた。この程度なら無視して次へ進みたい。`--analyze` は未実行だった。
- `--analyze` と時系列で判定: 触れ始め HAA −8.5°、止まったのは −10.3 / −11.0°、R1・R2 で同じ。結果は `reports/2026-09-23_d10-9_result.md`。本人の判断で許容して床上へ。
- D10-10 を実装: 送信器 `--floor-limits`（KFE・FFE 6.0 A、保持 ≤30 s、2 s ごとの電流表示。build `D10_10_FLOORSTAND_20260923_1600`、テスト 143本 OK）。手順 `procedures/d10_10_吊り紐つき床上立ち_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-10_吊り紐つき床上立ち.md`。D10-9 の引き継ぎは `inactive/` へ移した。

## 続き: D10-10 の判定と D10-11 の実装

- ユーザー所見: 0x11・0x1B・0x2B のねじがかなり緩んでいた。降ろしたあとは紐をたるませて、手で左右から支えた。それでも主に後ろへ倒れる。本人の仮説は「初期姿勢で膝を曲げているから静止しにくい」。R1 は1回、R2 は2回。`--analyze` は未実行だった。
- 判定（`reports/2026-09-23_d10-10_result.md`）: 足首はつま先が上がる向きに押され、方策段階は後ろへ 14〜20°傾いた。方策は 0.3 s・2.0 s で停止（FFE 6 A の中止と推定）。URDF の重心は足首より 1.5 cm 前で、膝を伸ばしても 1.0 cm。膝の曲げより、実機の重心がモデルより後ろにあることが原因と見た。AK80-9 の CSV の電流の符号が誤差と一致しないことも見つけた。
- ユーザーは「D10-10 を作り」と書いたが、D10-10 は実行済みだったので、次の実験を D10-11 として作った。送信器 `--stand-knee-deg` / `--stand-lean-deg` / `--lean-sweep-deg`（build `D10_11_LEANSWEEP_20260923_1700`、テスト 150本 OK）。手順 `procedures/d10_11_床上前傾スイープ_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-11_床上前傾スイープ.md`。

## 続き: D10-11 を待たずに D10-12

- ユーザー: 時間がないので D10-11 を待たずに進めたい。D10-11 は消さずに残し、新しい実験の実装・手順を作り、前の引き継ぎ書に追記してほしい。結果は次のチャットに貼る。
- D10-12 を実装した: `--auto-lean-deg`（保持中に前傾を自動で合わせる）、`--walk-limits`（KFE・FFE 10 A、速度 200°/s、slew ≤200°/s）。build `D10_12_AUTOLEAN_WALK_20260923_1730`、テスト 155本 OK。
- 手順 `procedures/d10_12_自動前傾_床上方策_実験手順.md`（R1 vx 0 floor、R2 vx 0 walk、R3 vx 0.2 walk）。D10-11 の引き継ぎ書（active のまま）の末尾に追記。CONTEXT と README も更新。

## 続き: 前傾合わせを飛ばして D10-13

- ユーザー: 釣り合う前傾を飛ばしたら無理なのか？ このあとどんな実験をするのか？ 本質的な実験を先にやり、無理なら戻りたい。
- 答え: 飛ばせる。方策は自分で釣り合いを取るように学習していて、D10-10 で倒れたのは方策の踏み込みを安全装置（FFE 6 A、slew 30°/s）が止めたため。前傾合わせは補助で、前提ではない。
- D10-13 を実装: `--walk-limits` のとき duration ≤15 s、`--command-delay-seconds`。build `D10_13_FLOORWALK_20260923_1800`、テスト 158本 OK。手順 `procedures/d10_13_床上方策_立ちと歩行_実験手順.md`（R1 立つ、R2 vx 0.2、R3 vx 0.4、戻り方、このあとの道筋）。
- 引き継ぎを `handoffs/active/2026-09-23_d10-13_床上方策_立ちと歩行.md` に作り直した。D10-11 の引き継ぎは inactive へ。D10-11・D10-12 は戻り先として残した。
