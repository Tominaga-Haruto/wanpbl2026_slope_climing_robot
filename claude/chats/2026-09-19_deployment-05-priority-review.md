# 2026-09-19 D5 着手条件と優先度の確認

## やったこと

- リポジトリの README、`project_handbook.md`、平地デプロイのロードマップ、D1・D5・D6の指示書、コントローラー準備書を確認した。
- D1成果物が作業ツリーに受領済みかを、`obs_contract.md`、ONNX、golden dataset、DEPLOY_READMEの名前で確認した。

## 決めたこと・分かったこと

- D5でスティックの前後・左右を `vx` / `vy` に割り当てるには、D1の `obs_contract.md` にある `velocity_commands` の順番・base座標の向き・実値範囲が必要である。推測で符号を決めない。
- 現在の作業ツリーにはD1のデプロイ成果物が見当たらない。そのため、D5の最小接続より先に、採用済みの `H_eff13p5@2999` をD1の形式で一度だけエクスポートして受領するのが優先である。
- D1は再学習や追加評価ではなく、既存checkpointからONNX、観測契約、golden dataset、アクチュエータ表、SHA256、DEPLOY_READMEを出す作業である。
- D5のうち、コントローラーから固定のゼロ指令を入力境界に渡す骨組みだけは先行可能だが、非ゼロの速度指令と符号の確定はD1後に行う。

## 手を動かした場所

- 文書の確認のみ。実機、CAN、コントローラー、方策ファイルには変更なし。

## 積み残し・次にやること

1. 学習側でD1を実施し、成果物一式をノートPC側へ受け渡す。
2. `obs_contract.md` でD5の速度指令の順序・符号・範囲を確認する。
3. ver9入力境界へ、固定コマンドとコントローラー入力を切り替えて渡す最小部品を接続する。
4. デッドマン、途絶停止、変化率制限などはD5の対象外として後続のコントローラー安全化で実装・乾式試験する。
