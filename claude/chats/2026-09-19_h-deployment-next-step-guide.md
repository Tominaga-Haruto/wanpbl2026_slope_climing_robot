# 2026-09-19 Hデプロイの次工程案内

## やったこと

- `README.md`、`project_handbook.md`、現行の実機向け引き継ぎ書 `next_chat_briefing_motor.md` を読み、D1後の進行順を確認した。
- 未追跡の `H_eff13p5_2999\H_eff13p5_2999\` を読み取り専用で確認し、`SHA256SUMS.txt` の必須6ファイルを再計算した。

## 決めたこと・分かったこと

- D1は完了済み。6必須ファイル（`policy.onnx`、`obs_contract.md`、`golden.npz`、`verify_onnx.md`、`actuator_table.md`、`DEPLOY_README.md`）のSHA256はすべてmanifestと一致した。
- M4は独立タスクにしない。2機種の実測済み係数は将来のMIT送信時に参照し、次の主線はM7のCANなしgolden再生・ONNX乾式ループである。
- D5の左スティック `(vx, vy, 0)` は、M7の固定コマンド入力を残したまま差し替え可能な入力部品として後続で扱う。速度指令の単位・符号は成果物に明記がないため、正本との照合前に固定しない。
- CAN、モーター、T265、コントローラーへの接続・送信はこの段階では行わない。

## 手を動かした場所

- 読み取り確認のみ。成果物ディレクトリや実機制御リポジトリは変更していない。

## 積み残し・次にやること

1. M7: `golden.npz`を入力にONNX action照合、目標角計算、50 Hz周期ログだけを行う乾式ループを実装する。
2. D5: 固定コマンドと併存する左スティック入力を実装し、正本との照合後にだけ `(vx, vy, 0)` の符号・単位を確定する。
