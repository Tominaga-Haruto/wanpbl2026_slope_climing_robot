# 2026-09-23 D10-3 の判定と D10-4 の準備（Cowork、2往復）

## 入力
- D10-3 の R0×2・R1・R1（足閉じ）・R2・R3・R4 を回した。本人所見: R1 は少し足が閉じた、R3・R4 は左足が後ろ・右足が前に交差してやさしくゆっくり動いた。
- 2往復目で: 全 run で脚を手で軽く支えていた（足が閉じないように、動き出しは任せる）。最後の vx 0.4 の run は遊びで、CSV は無視してよい。画面ログ全文。

## 判定（保存CSVの `--analyze` と画面ログ）
- R1・R2 の hold は 3 s 完走、保持 ≤0.10 A（判定2）。手の支えがあるので確定ではない。
- R3/R4 は ramp 完走後、方策 1〜2 tick 目に電流中止（0x22 4.30 A／0x1A 4.35 A）。原因は、方策段階の slew が 0x1C だけに掛かっていたこと（送信器 1093〜1100 行）。
- D10-2 報告の「1.0 A が自重より小さい」を訂正（報告書冒頭に1行）。

## 決めたこと・実装
- 送信器 build `D10_4_ALLSLEW_20260923_1200`: `--policy-slew-dps`（全軸、`--all-axes` の方策 run で必須）、全軸プレアーム・ゲート（|目標|>40° / |delta|>30°）、`POLICY STAGE`・`SLEW GAP` の表示、`--analyze` の asked 修正。テスト 83本 OK。
- 次は D10-4: 手を離して hold を取り直し → slew 20 °/s・vx 0 → slew 30 °/s・vx 0.2。床上はまだ。

## 変更したファイル
wanpbl: `claude/reports/2026-09-23_d10-3_result.md`（新規）、`claude/reports/2026-09-23_d10-2_result.md`（訂正1行）、`claude/procedures/d10_4_全軸slew_吊り方策_実験手順.md`（新規）、`claude/procedures/d10_3_...`（完了1行）、`claude/procedures/README.md`、`claude/handoffs/active/2026-09-23_d10-4_全軸slew吊り方策.md`（新規）、d10-3 引き継ぎを inactive へ、`claude/CONTEXT.md`、この記録。
Connect2USB2CAN: `ver9_d8_sender.py`、`test_ver9_d8_sender.py`。

## 3〜4往復目（D10-4 の結果 → D10-5）
- D10-4: R1/R1b は手を離して保持 ≤0.05 A（脚が閉じ切った位置）。R2 1回目はゲートが拒否（LR_HAA delta −31.9°）、以降は脚を閉じたまま `oa`。R2・R3 は方策段階を完走（初の全身ループ）。本人所見: 左つま先と右かかとがぶつかる。
- 判定: 閉じた脚での `oa` で HAA 原点が左約6°・右約12°ずれ（D10-3 ログから推定）、全軸 0°スタートは分布の外（方策が KFE +178° を要求）。報告 `reports/2026-09-23_d10-4_result.md`。
- 本人提案「閉じた状態から初期姿勢にしてから歩かせる」を採用。本人指摘「`oa` 後に手を離す必要はない」も採用: 基準姿勢で持ったまま `oa`、run の ramp 開始まで持つ。
- 実装: build `D10_5_STANDPOSE_20260923_1300`（`--stand-seconds`・`--stand-only`、テスト 89本 OK）。手順 `procedures/d10_5_基準姿勢から方策_実験手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-5_基準姿勢から方策.md`。

## 5往復目（D10-5 の結果 → D10-6）
- D10-5: R1 ×2・R2・R3 すべて完走（中止なし）。初期姿勢の保持電流を実測（LL_HFE 1.80 A など）。本人所見: 左右に同じ角度を送ると脚が前後に割れ、終了時や方策段階で足がぶつかる。
- 判定: エンコーダ上は両脚とも同じ角度。シムの URDF（myrobot_dummy）では HFE・KFE・FFE の軸が左右同じ向き → 片脚のピッチ3軸の実機→H 符号が −1 の疑い（D4 は全軸 +1）。報告 `reports/2026-09-23_d10-5_result.md`、D4 報告に要確認1行。
- 本人提案（ずっと左右を手で支える／HAA 固定・可動域を狭める）は、符号を直してもぶつかる場合の吊り診断専用として保留。
- 次: 無通電の符号確認 `procedures/d10_6_関節符号_無通電確認手順.md`、引き継ぎ `handoffs/active/2026-09-23_d10-6_関節符号確認.md`。ここでチャットを引き継ぐ。
