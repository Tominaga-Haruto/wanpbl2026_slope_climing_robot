# 2026-09-19 Hデプロイ成果物の受領・照合

## やったこと

- WRS機で抽出したH_eff13p5@2999のデプロイ成果物を、ノートPCの `H_eff13p5_2999\H_eff13p5_2999\` で確認した。
- `SHA256SUMS.txt` の6必須ファイルをノートPC側で再計算し、WRS側manifestと全件一致を確認した。
- `obs_contract.md`、`actuator_table.md`、`verify_onnx.md` を読み、デプロイ統合に必要な最小値を整理した。

## 決めたこと・分かったこと

- H_eff13p5@2999が実機デプロイの第一候補のまま。ONNXは42入力・10出力で、Torch照合はPASS（random最大誤差 `9.537e-06`、実観測最大誤差 `7.153e-07`）。
- 50 Hz、42観測、10行動、行動scale 0.5、目標角=既定角+0.5×行動を実物の成果物で確認した。
- `velocity_commands` の順は `(lin_vel_x, lin_vel_y, ang_vel_z)`、範囲は各 `[-1.0,+1.0]`。成果物には単位の明記がないため、次チャットで推測により固定しない。
- 次は実機CAN接続ではなく、M4の機種別Kp/Kd換算表とM7のCANなし乾式ループ。重みをGitへ追加しない。

## 手を動かした場所

- `H_eff13p5_2999\H_eff13p5_2999\` のファイル一覧、manifest、SHA256、観測契約、アクチュエータ表、ONNX検証結果を読み取り確認した。
- `project_handbook.md` と `next_chat_briefing_motor.md` を現在地に合わせて更新した。

## 積み残し・次にやること

- M4: 全10関節のシムゲインをAK80/AK10の実測係数でMIT指令Kp・Kdへ換算し、上限案を作る。
- M7: golden.npzを使うCANなし乾式のONNX再生・目標角計算・周期測定を実装する。
- 実機観測の単位・符号・欠損時停止、M5原点表、T265の途絶停止を確定してから、ユーザーの明示のもとでのみCAN接続へ進む。
