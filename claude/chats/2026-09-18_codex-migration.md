# 2026-09-18 Codex への引き継ぎ設計

## やったこと

- `README.md` と `next_chat_briefing.md`（09-18 朝版）を読み、現在地を確認。
- ユーザーから「週間制限に到達した。発表まであと1週間、Codex に移行する。Claude はほぼ使わない。引き継ぐには何をすればいいか」と依頼。
- WRS 側の朝の状況報告（03:20〜09:38 の6時間空回り、仮デプロイ用パッケージ完成、T1/T2 完走、P1 未再起動、2巡目の指示待ち）も同時に受け取った。
- 引き継ぎ手順を `claude/codex_migration.md` に新設。リポジトリ直下に置く入口ファイル `AGENTS.md` を作成してユーザーに配布。
- WRS機で `claude/` の存在を確認 → **存在しなかった**ので `codex_migration.md` を書き直し、Codex / ChatGPT の使い分けと立ち上げ手順を追加した。

## 決めたこと・分かったこと

- **★ リポジトリに `claude/` は無い（2026-09-18 実測）。** `dir : Cannot find path 'D:\\Tominaga\\slope-climbing-robot\\claude'`。
  **66 本の md は claude.ai のプロジェクトの中にしか無い** → 書き出しが引き継ぎの本体で、他の全作業より先。
  （前半で「リポジトリにも入っている公算が高い」と書いたのは**外れ**。パスの付き方からの推測だった。）
- 同時に判明: `.bak` 類・`references/`・`tools/view_*.png`・`onshape_export/...bak_*` が大量に未追跡。push には含まれない（`??`）が、`docs/experiments/exp09_turn_reproducibility.md` は成果物なので commit する。`.gitignore` に `*.bak_*` を足す。
- 書き出しの優先度を Tier 1（13 本・現在地と正本）/ Tier 2（8 本・判断の根拠）/ Tier 3（`chats/` と実験指示書・1週間の作業には不要）に分類。方法は ①claude.ai の画面から手コピー（消費ゼロ）②haiku の並列サブエージェントに逐語書き出しさせて zip（親の文脈が増えない＝いちばん安い）③Sonnet の新チャット、の順。**Opus では始めない。**
- **ChatGPT のプロジェクトと Codex CLI の使い分け:** 作業の本体は Codex CLI（ファイルを触れる・`AGENTS.md` を自動で読む）。ChatGPT のプロジェクトは機械に触れないので、相談・採点役・発表資料に限る。**md を両方に置かない** ── 二重管理は claude.ai でいま起きている状態を作り直すだけ。発表資料だけは例外。
- 引き継ぎで**失われるのは「実行と講評の二重化」**。停止点4 の G 推しの食い違い、実験06 の起動行の欠け、「合格1点を候補にしない」判断は、いずれも外から見ていたから出た。埋め方は ①判定規則を走らせる前に書く ②採点だけ実行権のない別セッション ③夜間の自走をやめる。
- ノートPC の `C:\\Users\\harut\\Connect2USB2CAN` は git の外 → リポジトリの `hardware/connect2usb2can/` へ**コピー**（移動しない）。`.venv310` と `logs` は入れない。重み（`deploy_pkg_20260918.zip`）は USB で運び SHA256 照合。
- **発表に向けた割り切り（Cowork の推奨）**: 方策は H_eff13p5@2999 で凍結、その場旋回は打ち切って「未達・原因は特定済み」として発表、今日回すなら実機の弱点（stiffness ×0.7 で転倒 6.2%）を直接潰す P1（`P_gainDR_narrow`）だけ、旋回の2巡目（`O_turn35` / `O_w2`）は回さない、保険の動画を先に撮る。

## 手を動かした場所

- 新設・改訂: `claude/codex_migration.md`（§1 を実測で書き直し、§5 使い分け・§6 立ち上げ手順を追加）
- 新設（リポジトリ直下に置く想定でユーザーに配布）: `AGENTS.md`
- 読んだだけ: `claude/README.md`, `claude/next_chat_briefing.md`

## 積み残し・次にやること

- **md 66 本の書き出しが未実施。** これが終わるまで引き継ぎは成立しない。
- `claude/README.md` の索引更新は**やっていない**（README 全文の書き直しになりトークンを食うため、制限状況を優先して省いた）。書き出し後、Codex 側で `codex_migration.md` の1行を足す。
- `training_runs.md` は停止点4 から止まったまま。実験06〜09 と夜間の結果は未記入。
- WRS の 2巡目の起動可否はユーザー判断待ち（Cowork の推奨は P1 のみ）。
- `codex_migration.md` §8 のチェックリストが、そのまま次にやることの一覧。
